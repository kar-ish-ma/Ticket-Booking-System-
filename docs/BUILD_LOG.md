# Ticket Booking System — Build Log

> **The single source of truth for what is done, in flight, and next.** Read this at the start of every session; update it at the end of every task. Nothing gets built that isn't a row here.

**Status key:** ⬜ Not started · 🟨 In progress · ✅ Done · ⛔ Blocked · ❌ Dropped (with reason)

**Task ID format:** `P{phase}-{n}`. Reference the ID in every commit: `feat(holds): atomic acquire [P3-2]`

**Stack:** Node 20 + Express 5 + PostgreSQL (`pg`, raw SQL) · React 18 + Vite · plain JavaScript, ESM.

**No Docker, no Redis, no containers.** PostgreSQL is the only service. Dev machine: Windows, 4 GB RAM.

### Environment status (already done, do not redo)

| Item | Status |
|---|---|
| PostgreSQL 16.3 | ✅ installed, running as a Windows service |
| Databases `ticket_booking`, `ticket_booking_test` | ✅ created |
| `postgres` password | ✅ set; `pg_hba.conf` back on `scram-sha-256` |
| Node 22.19 | ✅ |
| Git 2.51 | ✅ |
| Claude Code | ✅ via VS Code extension |

`DATABASE_URL=postgresql://postgres:<password>@localhost:5432/ticket_booking`

---

## Progress at a glance

| Phase | Theme | Tasks | Done |
|---|---|---|---|
| 0 | Foundation and tooling | 7 | 7/7 |
| 1 | Database and auth | 7 | 7/7 |
| 2 | Venues, events, shows, seat map | 7 | 7/7 |
| 3 | **Seat holds, TTL, concurrency** ⭐ | 9 | 5/9 |
| 4 | Booking, QR, email | 9 (1 dropped — P4-6) | 4/9 |
| 5 | **Waitlist and time-limited offers** ⭐ | 8 (3 dropped — P5-6/7/8) | 5/8 |
| 6 | Realtime seat map | 5 | 0/5 |
| 7 | Static client (vanilla) | 10 (9 dropped — D-53) | 1/10 |
| 8 | Reports, admin, check-in | 5 | 0/5 (P8-1 partial — see its row) |
| 9 | Hardening and proof | 6 | 0/6 |
| 10 | Docs, deploy, demo | 7 | 0/7 |
| | **Total** | **80** (13 dropped — P4-6, P5-6/7/8, P7-2..P7-10) | **36/80** |

---

## Phase 0 — Foundation and tooling

| ID | Change | Status | Files touched | Verified by | Commit |
|---|---|---|---|---|---|
| P0-1 | npm workspaces: `server/`, `client/`, `shared/`; ESM everywhere | ✅ | `package.json` (root, `server/`, `client/`, `shared/`), `.gitignore`, `docs/FILE_MANIFEST.md` (relocated from repo root) | `npm install` at root: 0 vulnerabilities, `npm ls --workspaces` resolves all three via symlink | |
| P0-2 | ESLint + Prettier + `eslint-plugin-jsdoc`; `jsconfig.json` with `checkJs` | ✅ | `eslint.config.js`, `jsconfig.json`, `.prettierrc.json`, `.prettierignore`, `package.json` (lint/format scripts + devDependencies), `docs/FILE_MANIFEST.md` | `npm run lint` → exit 0, zero warnings on the current tree; jsdoc rule verified live by adding a throwaway undocumented exported function (2 warnings fired) then deleting it | |
| P0-3 | ~~Install Postgres~~ **already done by hand** — Postgres 16.3 running, `ticket_booking` + `ticket_booking_test` created, password set. Remaining: verify `shared_buffers=128MB`/`max_connections=50` in `postgresql.conf`, add `.gitattributes` for LF endings, and write the setup steps into `docs/DEPLOYMENT.md` so a grader can reproduce them | ✅ | `.gitattributes`, `docs/DEPLOYMENT.md`, `docs/FILE_MANIFEST.md`, `C:\Program Files\PostgreSQL\16\data\postgresql.conf` (outside repo, not committed) | `postgresql.conf` read directly (no DB credentials needed): `shared_buffers = 128MB` already matched the target, no edit needed. `max_connections` edited 100→50 on disk, confirmed by re-reading the file. ⚠️ **Not yet live**: applying it needs a full service restart, and the coding session's shell isn't running elevated, so `Restart-Service postgresql-x64-16` failed with a permissions error — needs to be run manually from an elevated PowerShell. `work_mem` left untouched: commented out, and Postgres's built-in default already equals the 4MB target. DB existence (`ticket_booking`, `ticket_booking_test`) not re-verified live — `pg_hba.conf` is `scram-sha-256` with no passwordless path on Windows, and pasting the DB password into chat wasn't worth it for a fact the environment-status table above already asserts; cross-checked instead via `data/base/` having 10 database directories (3 stock + 7 more, consistent with but not proof of the two project DBs). | |
| P0-4 | Express app skeleton: helmet, CORS+credentials, cookie-parser, `pino-http`, error handler last | ✅ | `server/src/app.js`, `server/src/index.js`, `server/src/middleware/errorHandler.js`, `server/src/modules/health/health.routes.js`, `shared/errors.js`, `server/package.json` (deps), `eslint.config.js` (`argsIgnorePattern`), `docs/FILE_MANIFEST.md` | `GET /health` → `200 {"success":true,"data":{"status":"ok"},"error":null}` — verified live against a real running process, not just read. `GET /nope` → `404 NOT_FOUND` envelope. Error handler proven live: a throwaway route that threw was added, curled (→ `500 INTERNAL_ERROR`, no stack leaked, `requestId` present), then removed before commit — same falsification instinct as `prove-race`, applied since no Vitest/Supertest infra exists yet to keep the test as a fixture. `npm run lint` clean. | |
| P0-5 | Zod env validation; `.env.example` written first, fully commented | ✅ | `.env.example` (repo root), `server/src/config/env.js`, `server/src/app.js` (CORS origin now reads `env.WEB_URL`), `server/src/index.js` (listens on `env.PORT`), `server/package.json` (`zod` dep), `docs/DEPLOYMENT.md` (fixed `server/.env` → repo-root `.env`, matching `.env.example`'s actual location) | Booted with no `.env` at all → exit 1, readable report naming all 4 missing required vars (`DATABASE_URL`, `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`, `QR_SIGNING_SECRET`). Booted with those 4 set → defaults applied correctly (`PORT=3000`, `WEB_URL=http://localhost:5173`, `SEAT_HOLD_TTL_SECONDS=600`, ...) and the full server served `GET /health` → 200 through the validated `env` object, not a raw `process.env` read. `QR_SIGNING_SECRET` set equal to `JWT_ACCESS_SECRET` → exit 1 citing D-10 by name, proving that check is live, not just documented. `npm run lint` clean. | |
| P0-6 | Swagger via `swagger-jsdoc` at `/api/docs` | ✅ | `server/src/config/swagger.js`, `server/src/app.js`, `server/src/modules/health/health.routes.js` (`@openapi` annotation), `server/package.json` (deps), `eslint.config.js` (`@openapi` allowed as a defined JSDoc tag) | Verified live, not just "a page loads": `/api/docs.json` → `paths: {"/health": {...}}`, matching `health.routes.js`'s annotation exactly. `/api/docs/` → 200. Caught and fixed a real bug in the process — see D-19. `npm run lint` clean. | |
| P0-7 | CI workflow: install → lint → test against GitHub's Postgres service container (Linux runners only — never the dev machine) | ✅ | `.github/workflows/ci.yml`, `docs/FILE_MANIFEST.md` | YAML parsed and validated with `js-yaml` before pushing (not just eyeballed). `npm ci`, `npm run lint`, and `npm test --if-present` all run locally with the exact commands the workflow uses — all exit 0. **CI confirmed green by the user**: run #1 on `phase/0-foundation`, commit `9d11e0f`, 48s (I have no `gh` CLI here to check this myself). | |

**Exit criteria:** `npm run dev:server` gives a booting API with Swagger and a passing health check, with only Postgres installed.

---

## Phase 1 — Database and auth

| ID | Change | Status | Files touched | Verified by | Commit |
|---|---|---|---|---|---|
| P1-1 | `node-pg-migrate` set up; migrations 001–002 (enums, users, venues, categories, seats, events, shows) | ✅ | `server/src/db/migrations/001_init.sql`, `002_events_shows.sql`, `server/src/db/migrate.js`, `server/package.json` / root `package.json` (migrate scripts), `server/src/config/env.js` (SMTP_PORT fix, D-23) | `npm run db:migrate` applied all 7 migrations clean against the real `ticket_booking` DB; full down-to-zero (all 7, reverse order) then back up also clean — output in `docs/TESTING.md`. Discovered live that `node-pg-migrate`'s CLI `--envPath` silently no-ops without `dotenv` installed — resolved with a programmatic `runner()` wrapper (D-22) instead of adding the dependency back. | |
| P1-2 | Migrations 003–006 (`show_seats`, holds, bookings, waitlist, outbox, audit) with all indexes commented | ✅ | `server/src/db/migrations/003_show_seats.sql`, `004_bookings.sql`, `005_waitlist.sql`, `006_outbox_audit.sql` | `information_schema`/`pg_constraint` queried live against the real DB: `show_seats` has `UNIQUE (show_id, seat_id)`, both `expires_at` and `reserved_until` (D-14), and the `booking_id` FK (deferred from 003 to 004 — `bookings` doesn't exist until 004, see 003's file header) landed correctly. All 18 application tables present after `db:migrate`. | |
| P1-3 | ⭐ `pool.js` + `withTransaction.js` with statement timeout and guaranteed `client.release()` | ✅ | `server/src/db/pool.js`, `withTransaction.js` | Verified live against the real DB, not a unit test with a mocked pool (no test runner exists yet — Vitest is a later task): a thrown error inside `withTransaction` rolled back a real inserted row (visible before the throw, gone after), and `pool.idleCount` returned to 1 — not just `totalCount`, checked per-transaction — after each of 6 consecutive failed transactions. Full output in `docs/TESTING.md`. | |
| P1-4 | ⭐ `job_queue` table + `enqueueJob(client, ...)` + poller with `FOR UPDATE SKIP LOCKED`, backoff, DEAD after N attempts | ✅ | `server/src/db/migrations/007_job_queue.sql`, `server/src/queue/enqueue.js`, `poller.js`, `server/src/utils/backoff.js`, `server/src/index.js` (wires `startPoller({})`) | Two `claimJobs()` calls fired genuinely concurrently (`Promise.all`) on one shared job row, 21 times total (1 detailed + 20-iteration stress loop): exactly one claim every single time, `doubleClaims=0 zeroClaims=0`. Full output in `docs/TESTING.md`. | |
| P1-5 | Auth: register, login (argon2), access+refresh httpOnly cookies, rotation with reuse detection | ✅ | `server/src/modules/auth/{auth.routes,controller,service,queries}.js`, `shared/schemas/auth.schema.js`, `server/src/middleware/validate.js`, `server/src/utils/errors.js`, `server/src/middleware/errorHandler.js` (now maps `DomainError`), `server/src/db/migrations/008_refresh_tokens.sql`, `shared/errors.js` (5 new auth codes), `server/src/app.js` (mounts `/api/v1/auth`) | No Vitest yet, so verified live against the real DB via a direct `auth.service.js` script (not HTTP, to remove doubt about what's exercised) — full output in `docs/TESTING.md`. Register → login → `/me` → refresh (rotation, new token differs, same `family_id`) all correct. Reuse detection (replaying an already-rotated token) genuinely revokes the whole family and a second attempt with the "new" token also correctly fails — this took two real bug fixes to get right, see D-25 and D-26. Duplicate email → 409, wrong password → 401, short password/bad email → 422, all with the right codes. **Post-close-phase-audit fix**: refresh-token expiry now checked via Postgres's own `now()` (D-29), not the app clock — re-verified live: rotation and reuse detection still correct, and a token back-dated directly in Postgres (independent of any JS date arithmetic) is correctly caught as expired. | |
| P1-6 | `requireAuth`, `requireRole`, `requireOwnership` middleware | ✅ | `server/src/middleware/{requireAuth,requireRole,requireOwnership}.js` | Verified live over real HTTP (two temporary routes added to `app.js`, exercised, then removed before commit — same pattern as the P0-4 error-handler proof, since no `events` table exists yet to test ownership against for real). All 5 outcomes correct: no session → 401; customer on an organiser-only route → 403; organiser on that route → 200; organiser on their own synthetic resource → 200; organiser on someone else's → 403. Full output in `docs/TESTING.md`. | |
| P1-7 | Seed skeleton: users for all three roles; idempotent | ✅ | `server/src/db/seed.js`, `server/package.json` / root `package.json` (`db:seed`) | `npm run db:seed` run twice: first run creates all 3 accounts, second run reports all 3 already-exists and skips, both exit 0. Idempotency comes from the `UNIQUE(email)` constraint + `ON CONFLICT DO NOTHING`, not an application-level existence check. | |

**Exit criteria:** Three roles log in; role **and ownership** are enforced and tested.

---

## Phase 2 — Venues, events, shows, seat map

| ID | Change | Status | Files touched | Verified by | Commit |
|---|---|---|---|---|---|
| P2-1 | Venue CRUD with `layout_meta` (rows, cols, aisles, stage position) | ✅ | `modules/venues/venues.{routes,controller,service,queries}.js`, `shared/schemas/venue.schema.js` | Live against the real DB/HTTP: `POST /venues` created "Test Cinema" with a `layoutMeta` object round-tripping intact; `GET /venues/:id`, `GET /venues` both correct; `GET /venues/:id` for a random uuid → `404 NOT_FOUND`. Full transcript in `docs/TESTING.md`. | |
| P2-2 | Seat categories per venue with colour and sort order | ✅ | `venues.queries.js#insertCategory/listCategoriesByVenue` | Live: created "Premium" and "Standard" on the test venue; re-POSTing "Standard" → `409 CONFLICT` (the `UNIQUE (venue_id, name)` violation caught and translated, not a raw 500). | |
| P2-3 | Bulk seat creation from a row spec; reject duplicate grid coordinates | ✅ | `venues.queries.js#insertSeatsBulk`, `venues.service.js#bulkCreateSeats` | Live: one `POST .../seats/bulk` call with 2 row specs created 10 seats (rows A and B) via a single `unnest()` INSERT; a follow-up row spec deliberately colliding on `gridRow: 1` → `409 CONFLICT`, zero seats written (transaction rolled back, not a partial insert). | |
| P2-4 | Event CRUD (ownership-guarded) + public browse with filters and pagination | ✅ | `modules/events/events.{routes,controller,service,queries}.js`, `shared/schemas/event.schema.js`, `server/src/middleware/validate.js` (Express 5 fix, see D-31) | Live: organiser created an event; a second organiser's `PATCH` on it → `403 FORBIDDEN` (`requireOwnership`); browse with `type`+`city` matched, `city=Nowhere` and `q=Nonexistent` both correctly returned empty; malformed `?page=notanumber` → `422 VALIDATION_ERROR`, not a crash. Two real bugs found and fixed live — see D-31 (Express 5's `req.query` has no setter) and D-32 (`.partial()` doesn't strip Zod `.default()`, so a bare `PATCH {isPublished:true}` was silently wiping `description`). | |
| P2-5 | Show creation with per-category pricing and per-show `hold_ttl_seconds` | ✅ | `modules/shows/shows.{routes,controller,service,queries}.js`, `shared/schemas/event.schema.js` | Live: created a show with 2 category prices in one transaction; omitted `holdTtlSeconds`/`offerTtlSeconds` correctly defaulted to 600/900 (`shows` table's own column defaults) after fixing D-30. `GET /shows/:id` returned the show with its event/venue/prices flattened into one response. | |
| P2-6 | ⭐ `publishShow()` materialises one `show_seats` row per venue seat, one batched insert in a transaction | ✅ | `shows.service.js#publishShow`, `shows.queries.js#materialiseShowSeats/countShowSeats` | Live: publishing the 10-seat test venue's show created exactly 10 `show_seats` rows, all `AVAILABLE` (`seatCount: 10` in the response, cross-checked against the seatmap read below). Publishing the same show again → `409 CONFLICT` ("already been published"), not a duplicate-seat corruption — both the pre-insert `countShowSeats` check and, as a backstop, `UNIQUE (show_id, seat_id)` on a caught `23505` (see shows.service.js's WALKTHROUGH comment on the deliberate check-then-act, logged as Phase 2 debt below). | |
| P2-7 | ⭐ `GET /shows/:id/seatmap` returning **effective** state (expired HELD ⇒ available) | ✅ | `modules/seatmap/seatmap.{routes,controller,service,queries}.js` | **The literal proof requested**: manually ran `UPDATE show_seats SET state='HELD', expires_at = now() - interval '1 hour' ... RETURNING id, state, expires_at, (expires_at <= now()) AS is_past` directly against `ticket_booking` — raw result `{"state":"HELD","expires_at":"2026-08-23T18:52:14.296Z","is_past":true}`. Immediately after, `GET /api/v1/shows/:id/seatmap` reported that exact `showSeatId` as `"state":"AVAILABLE"`. Re-queried the raw row *after* the HTTP call to rule out anything else touching it: still stored as `HELD`, still in the past. No worker, poller, or cron exists yet (Phase 3) — the effective-state `CASE` in `seatmap.queries.js` is the only thing that could have produced `AVAILABLE`. Row reset to `AVAILABLE`/`NULL` afterward so it doesn't pollute Phase 3's starting state. Full transcript in `docs/TESTING.md`. | |

**Exit criteria:** A published show returns a complete seat map with live-derived states.

---

## Phase 3 — Seat holds, TTL, concurrency ⭐ **HIGHEST VALUE**

> Budget the most time here. Do not move to Phase 4 until every test in P3-9 is green three runs in a row.

| ID | Change | Status | Files touched | Verified by | Commit |
|---|---|---|---|---|---|
| P3-1 | 🔒 Seat state machine: transition map in `shared/seatStates.js`, `assertTransition()` on the server | ✅ | `shared/seatStates.js`, `server/src/modules/seatmap/seatState.machine.js`, `shared/errors.js` (+`ILLEGAL_SEAT_TRANSITION`), `server/src/utils/errors.js` (+`IllegalSeatTransitionError`), `server/tests/unit/seatState.machine.test.js`, `server/package.json`/root `package.json` (Vitest wired in, first automated test in the project) | `npm test` → **31/31 passed**: the full 5×5 = 25-pair matrix (every legal pair from §4.3 asserted not to throw, every other pair asserted to throw `IllegalSeatTransitionError`, including the `OFFER_RESERVED → OFFER_RESERVED` cascade self-loop), 2 unrecognised-state cases, and 4 deep-freeze immutability cases (outer map frozen, reassigning a key throws + doesn't mutate, every per-state array individually frozen, mutating one in place throws + doesn't mutate). **Falsified live**: temporarily deleted the `OFFER_RESERVED → OFFER_RESERVED` entry from `SEAT_TRANSITIONS` — exactly 1 test failed (`OFFER_RESERVED -> OFFER_RESERVED`, the removed pair, nothing else), proving the test actually exercises the map rather than passing vacuously; restored, re-ran, 31/31 green again. `npm run lint` clean. | |
| P3-2 | ⭐ Atomic acquire: `FOR UPDATE` CTE ordered by `seat_id`, expired-hold reclaim in the predicate | ✅ | `server/src/modules/holds/holds.queries.js` | Live against the real `ticket_booking` DB via a throwaway proof script (not committed — same pattern as Phases 0–2; the DB-backed Vitest harness is still P3-9's job per D-34): **(1)** AVAILABLE → acquired; **(2)** HELD not expired → rejected (empty array); **(3)** HELD expired → reclaimed (Layer 1); **(4)** `OFFER_RESERVED` with `expires_at` in the past but `reserved_until` in the future → rejected — **the literal D-14 check**; **(5)** `OFFER_RESERVED` with `reserved_until` also in the past → reclaimed; **(6)** a 2-seat request, one available + one already held → partial array (1 of 2), not an exception, proving `@throws never`; **(7)** a genuine `Promise.all` 2-way race on one available seat → exactly one winner, one empty array; **(8)** a genuine overlapping-set race `{X1,X2}` vs `{X2,X3}` via `Promise.all`, each wrapped in a throwaway "simulate P3-3" transaction (BEGIN → `acquireSeats` → rollback on shortfall else commit) — run **8 times**: every run, no deadlock, exactly one racer won both its seats, the other's raw result was a genuine partial array (e.g. just the uncontested seat) that the wrapper then rolled back to an effective zero, and the final DB state was always exactly one of `HELD,HELD,AVAILABLE` or `AVAILABLE,HELD,HELD` — never inconsistent, never half-held. Scenario 8 surfaced D-35. Full transcripts in `docs/TESTING.md`. `npm run lint` clean; existing 31/31 Vitest suite unaffected. | |
| P3-3 | ⭐ `createHold()`: all-or-nothing, ROLLBACK on partial, 409 with conflicting seat labels | ✅ | `server/src/modules/holds/{holds.service,holds.controller,holds.routes}.js`, `shared/schemas/hold.schema.js`, `shared/errors.js` (+`SEATS_UNAVAILABLE`), `server/src/utils/errors.js` (+`DomainError.details`, +`SeatsUnavailableError`, +`ValidationError`), `server/src/middleware/errorHandler.js` (reads `err.details` instead of hardcoding `null`), `server/src/app.js` (mounts `/api/v1/holds`), `holds.queries.js` (+`insertSeatHold`, +`findSeatLabels`) | Live E2E over real HTTP against a running server + the real `ticket_booking` DB (not the permanent Supertest/`testDb.js` harness — still P3-9's per D-34): **(A)** available seat → `201` with `hold.expiresAt` + `seatIds`. **(B)** one available + one already-held seat → `409 SEATS_UNAVAILABLE`, `error.details.conflictingSeats` correctly names only the held one; **`seatHoldsCountBefore === seatHoldsCountAfter === 0`** — the parent `seat_holds` row from the failed attempt is genuinely gone, not orphaned (the exact check requested); the available seat reverted to `AVAILABLE`, not left held. **(C)** 10-seat request against the `MAX_SEATS_PER_BOOKING=6` default → `422 VALIDATION_ERROR`. **(D)** the overlapping-set race `{X1,X2}` vs `{X2,X3}` over real concurrent HTTP (`Promise.all`), **run 6×**: every run, exactly one `201` (2 seats) and one `409` (conflicting seat always the contested one, X2), **`seatHoldsRowCount` always exactly 1** (only the winner's — never 0, never 2), and final DB state always exactly one of the two valid configurations. Full transcripts in `docs/TESTING.md`. `npm run lint` clean; existing 31/31 Vitest suite unaffected. | |
| P3-4 | ⭐ Idempotent `releaseHold(holdId, reason)` — double release is a no-op, never a throw | ✅ | `server/src/modules/holds/{holds.service,holds.controller,holds.routes,holds.queries}.js` (+`releaseHoldSeats`, `markSeatHoldReleased`, `findSeatHoldById`; mounts `DELETE /api/v1/holds/:id` with `requireOwnership`) | Live E2E over real HTTP, adapted to DB-state only (see note below — P3-6 owes the broadcast half): **(1)** create → `DELETE` → `200 {released:1}`, seat `AVAILABLE`, `hold_id` null, `seat_holds.status` → `RELEASED`; the SAME `DELETE` called 2 more times → `200 {released:0}` both times, no error, no further state change. **(2)** a non-owner's `DELETE` → `403 FORBIDDEN`, seat state completely unchanged. **(3)** `DELETE` on a nonexistent holdId → `404 NOT_FOUND`. **(4)** the addition from review — release a STALE holdId (A) whose seat was, in between, lazily reclaimed under a brand-new hold (B) via the real `createHold()`/`acquireSeats()` path: `DELETE`ing A returns `{released:0}` (A's predicate matches nothing — the seat's `hold_id` is now B); the seat's state is byte-for-byte identical before and after (still `HELD` by B); **A's own `seat_holds` row correctly transitions `ACTIVE → RELEASED`** (accurate — A's hold really is over); **B's `seat_holds` row is `ACTIVE` both before and after** — proving `markSeatHoldReleased`'s `id`-keyed predicate cannot cross-contaminate a different hold's bookkeeping row. Full transcripts in `docs/TESTING.md`. `npm run lint` clean; existing 31/31 Vitest suite unaffected. | |
| P3-5 | ⭐ Layer 2: `HOLD_EXPIRY` job enqueued **in the same transaction** as the hold | ⬜ **deferred to after Phase 5** — see below | `holds.service.js`, `queue/handlers/holdExpiry.js` | No hold can exist without its job row; release within ~2s of TTL | |
| P3-6 | ⭐ `pg_notify` publisher + `LISTEN` client bridging DB changes to Socket.IO rooms | ⬜ **deferred to after Phase 5** — see below | `notify/publisher.js`, `notify/listener.js` | Rolled-back TX broadcasts nothing — the key test | |
| P3-7 | Layer 3: `node-cron` 30s reconciler sweeping expired holds, independent of the poller | ⬜ **deferred to after Phase 5** — see below | `jobs/holdReconciler.job.js` | Stale row swept within 30s with the poller stopped | |
| P3-8 | Write the "Why there is no distributed lock" comment block + README section arguing the row lock is the boundary | ⬜ **deferred to after Phase 5** — see below | `holds.queries.js`, `README.md` | Reads as a decision, not an omission | |
| P3-9 | ⭐⭐ **Concurrency proof suite** — the 4 of PROJECT_PROMPT §6.6's six scenarios buildable through P3-4 (holds only; no bookings or waitlist/offers module exists yet), against local `ticket_booking_test`, plus the permanent Vitest/Supertest e2e harness itself. Grew at the Phase 3 close-out audit (below) with the write-side hold-reclaim test and the full `holds.test.js` P3-4 coverage the first pass had missed | ✅ | `server/vitest.unit.config.js`, `server/vitest.e2e.config.js`, `server/tests/setup/testEnv.js`, `testDb.js`, `fixtures.js`, `server/tests/e2e/concurrency.test.js`, `holdExpiry.test.js`, `holds.test.js`, `server/package.json` (test/test:unit/test:e2e/test:concurrency scripts, `supertest` devDependency), root `package.json` (`test:concurrency`), `.github/workflows/ci.yml` (3x concurrency loop), `docs/PROJECT_PROMPT.md` §6.6 (table annotated with where each scenario lives), `docs/TESTING.md`, `docs/FILE_MANIFEST.md` | Local run against the real `ticket_booking_test` DB: `npm run test:unit` 31/31, `npm run test:e2e` 8/8 (3 files). **Falsified live** (same convention as P3-1), three separate times: (1) `acquireSeats`'s full state predicate stripped to `WHERE s.id = c.id` — all 3 `concurrency.test.js` tests failed for the expected reason; (2) just the `(state='HELD' AND expires_at<=now())` OR-branch removed — the new write-side reclaim test in `holdExpiry.test.js` failed (`409` instead of `201`) while everything else stayed green, closing the gap the close-phase audit found (the phase's "seats free themselves" exit criterion previously had no test that depended on this exact branch); (3) `markSeatHoldReleased()`'s predicate changed from `WHERE id = $1` to `WHERE show_id = (SELECT show_id FROM seat_holds WHERE id = $1)` — `holds.test.js`'s stale-holdId test caught a live, unrelated hold's `ACTIVE` row silently flipping to `RELEASED`. All three predicates restored and confirmed byte-for-byte unchanged (`git status` empty) before being trusted. `npm run test:concurrency` run 3x in a row locally, 3/3 clean, matching the loop wired into CI. Full transcripts in `docs/TESTING.md`. The remaining two §6.6 scenarios needing bookings ("20 parallel confirms", "confirm at expires_at+1ms") move to Phase 4's `tests/e2e/bookingFlow.test.js`; "10 waitlisted users racing one offer" moves to Phase 5's `tests/e2e/waitlist.test.js` — deferred, not dropped, tracked in §6.6's own table and the Test debt section below. `npm run lint` clean. | |

**P3-4's verification is adapted, not shortchanged:** the task's original "Called 3× → one
broadcast, no error" assumed P3-6's `pg_notify`/`LISTEN` broadcasting exists — it doesn't yet
(deferred, see below). `releaseHold()` is proven entirely via direct DB-state inspection instead
(seat state, `seat_holds.status`, and — critically — that a *different* hold's row is untouched).
The "one broadcast" half of this task's original verification is still owed; P3-6 inherits it
when it's built.

**Ordering change (2026-08-24, user directive):** P3-5 through P3-8 are deferred to after Phase 5,
not dropped. Rationale: those four tasks make hold expiry *timely* and *visible* (a job that
releases within ~2s, a cron sweep, a live broadcast, and the README write-up arguing the design) —
they do not make it *correct*. Correctness is the SQL predicate alone, already proven at P2-7
(`GET /shows/:id/seatmap` reporting `AVAILABLE` for a manually-expired `HELD` row with zero
workers running) and re-confirmed at P3-2's `acquireSeats()` proof. P3-9's concurrency suite does
not depend on P3-5/6/7 existing — §6.6's "hold expiry with all workers stopped" test is
specifically designed to pass with Layer 1 alone. Sequencing P3-9 immediately after P3-4 gets the
headline scored proof (50-parallel-holds, overlapping sets, the full six-scenario suite) done
sooner, with Layers 2/3 and the realtime broadcast added once Phases 4/5 also need job-queue
handlers and `pg_notify` wiring of their own — one pass through that infrastructure instead of
building it early and revisiting it per phase.

**Exit criteria:** 50 parallel holds on one seat → exactly one winner, every run. Seats free themselves with all workers stopped.

---

## Phase 4 — Booking, QR, email outbox

**Ordering change (2026-08-24, user directive):** P4-2 (`confirmBooking()`) and P4-8
(`cancelBooking()`, redefined below to cover cancellation only) move ahead of the rest of Phase 4
and are built immediately before Phase 5, because Phase 5's cascade is built directly on top of
both: §7.2's cancellation→offer flow only exists as a continuation of `cancelBooking()`, and §7.3's
offer-acceptance reuses "the same guarded transaction shape as §6.5" — i.e. `confirmBooking()`'s
own `expires_at > now()` pattern. The rest of Phase 4 (P4-1, P4-3, P4-6, P4-7, and the
booking-history/PDF half of the old P4-8, split out below as **P4-9**) follows all of Phase 5.

**Real dependency found while planning this reorder, not just a preference:** `bookings.reference`
and `bookings.qr_token` are both `NOT NULL` (004_bookings.sql) — `confirmBooking()` cannot insert a
valid row without a reference and a QR token. Originally planned to pull P4-4/P4-5 forward in full
to satisfy this; **corrected by user directive** before P4-2 was built: P4-4/P4-5 stay skipped for
now, and `confirmBooking()` inlines minimal placeholder generators instead (see "Phase 4 debt"
below) — the real Crockford base32 generator and signed-JWT QR service land whenever P4-4/P4-5 are
actually revisited. `cancelBooking()`'s own spec (P4-8) still names "refund" explicitly, which is
why P4-1 (mock payments) stayed a real prerequisite and was built first. **Actual build order for
this stretch: P4-1 → P4-2 → P4-8 (cancellation half only) → all of Phase 5 → P4-3 → P4-4 → P4-5 →
P4-6 → P4-7 → P4-9.** Each still gets its own plan and its own commit — this is an order change,
not a batching license (CLAUDE.md's plan-mode rule for Phase 3/4/5 still applies to every single
row below).

| ID | Change | Status | Files touched | Verified by | Commit |
|---|---|---|---|---|---|
| P4-1 | Mock payment service: authorize → capture → refund | ✅ | `server/src/modules/payments/{payments.queries,payments.service}.js`, `server/tests/e2e/payments.test.js`, `docs/FILE_MANIFEST.md` | `npm run test:e2e` → 4 files, 10/10 (2 new, in `tests/e2e/payments.test.js`, not `tests/unit/` — see D-43). `authorizeAndCapture()` ends at exactly one `CAPTURED` row per booking, never left `AUTHORIZED`; `refund()` then a second `refund()` on the same booking returns `null`, no error. **Falsified live**: dropped `markPaymentRefunded`'s `AND status = 'CAPTURED'` guard — the double-refund test correctly failed (a row came back instead of `null`); restored, confirmed the file untracked-but-identical to what was written (new file, so no `git status` diff to check — re-read instead to confirm the restore matched the original). `npm run lint` clean; full suite (`npm test`) unaffected: 31 unit + 10 e2e. | |
| P4-2 | ⭐ `confirmBooking()`: hold→booking with the `expires_at > now()` guard; short `rowCount` ⇒ `410 HOLD_EXPIRED` | ✅ | `server/src/modules/bookings/{bookings.routes,bookings.controller,bookings.service,bookings.queries}.js`, `shared/schemas/booking.schema.js`, `shared/errors.js` (+`HOLD_EXPIRED`), `server/src/utils/errors.js` (+`HoldExpiredError`), `server/src/modules/holds/holds.queries.js` (`markSeatHoldReleased`'s status union widened to include `'CONVERTED'`), `server/src/app.js` (mounts `/api/v1/bookings`), `server/tests/e2e/bookingFlow.test.js`, `docs/FILE_MANIFEST.md` | `npm run test:e2e` → 5 files, 13/13 (3 new: happy path, confirm-at-TTL+1ms, 20-parallel-confirms). Happy path confirms seat `BOOKED`, `seat_holds` row `CONVERTED`, payment `CAPTURED`, one `booking_seats` row with the right `price_cents`. **§6.6 scenarios unblocked by this task, both proven**: confirm at `expires_at+1ms` → `410 HOLD_EXPIRED`, zero bookings/payments persist, seat stays raw-stored `HELD` (never touched, not reclaimed); 20 parallel confirms of one hold → exactly 1 `201`/booking/`CAPTURED` payment/`CONVERTED` hold, 19 × `410`. **Falsified live**: dropped `confirmHeldSeats()`'s `AND expires_at > now()` — the TTL test correctly failed (`201` instead of `410`); restored, re-verified. `assertTransition(HELD, BOOKED)` wired in immediately before the atomic UPDATE, closing the loop D-40 opened (see the new Decisions Ledger entry). Two inline placeholder generators stand in for P4-4/P4-5 — see "Phase 4 debt" below. `npm run lint` clean; full suite 31 unit + 13 e2e. | |
| P4-3 | Idempotency via the `bookings.idempotency_key` unique constraint — on conflict, return the original | ⬜ **deferred to after Phase 5** — see ordering note above | `middleware/idempotency.js`, `bookings.queries.js` | 20 parallel replays → 1 booking | |
| P4-4 | Booking reference generator (Crockford base32, no ambiguous characters) | ⬜ **deferred to after Phase 5** — see ordering note above. `confirmBooking()` (P4-2) inlines a placeholder generator in the meantime; see "Phase 4 debt" below | `utils/reference.js` | 100k generated, zero collisions | |
| P4-5 | ⭐ QR service: signed JWT (separate secret) → PNG buffer | ⬜ **deferred to after Phase 5** — see ordering note above. `confirmBooking()` (P4-2) inlines a placeholder token in the meantime; see "Phase 4 debt" below | `bookings/qr.service.js` | Decoded QR verifies against the secret | |
| P4-6 | ❌ Dropped (user directive, 2026-08-24 — Decisions Ledger D-54): ~~Transactional outbox: row + `OUTBOX_SEND` job both written in the booking transaction; 60s reconciler for orphaned rows~~ — superseded by P4-7 sending directly after commit instead | ⬜ | `queue/handlers/outboxSend.js`, `jobs/outboxReconciler.job.js` | — | |
| P4-7 | Nodemailer + EJS templates; QR inline via CID; **Ethereal** auto-account in dev, preview URL logged | ✅ **redefined (user directive, 2026-08-24): send directly after commit, no outbox/job-queue dependency (D-54)** | `server/src/mail/mailer.js`, `server/src/mail/templates/{bookingConfirmed,waitlistOffer}.ejs`, `server/src/modules/bookings/{bookings.service,bookings.controller}.js`, `server/package.json` (+nodemailer, ejs, qrcode), `server/tests/e2e/mail.test.js`, `docs/FILE_MANIFEST.md` | `npm run test:e2e` → 8 files, 30/30 (2 new). Both templates render, both actually send through a real Ethereal account (not mocked — CLAUDE.md's own "never mock the pool" convention extended to "never mock the mail transport either," since a mock would prove nothing about whether Nodemailer/EJS/`qrcode` genuinely compose into a sendable message), preview URLs asserted and logged. `sendBookingConfirmedEmail()` embeds a real QR PNG (from `booking.qrToken`, still P4-2's placeholder string — rendering is this task's job, not what the token IS) via CID. `sendWaitlistOfferEmail()` builds a real §7.3 claim link that currently 404s (P5-5 dropped) and a real deadline from `offer.expiresAt`. Verified live, not just via the dedicated test: the SAME run's log shows the automatic fire-and-forget sends firing from `bookings.controller.js` during ordinary `POST /bookings/confirm` and `POST /bookings/:id/cancel` calls elsewhere in the suite (5 total Ethereal preview URLs logged across the 2 dedicated test cases — 2 explicit calls the test makes plus 3 automatic ones from the HTTP endpoints those same tests also exercise), confirming the controller wiring works, not just the mailer module in isolation. **Falsified live**: broke `QRCode.toBuffer()`'s input (passed `null`), the booking-confirmation email test correctly failed (its own promise rejected), the waitlist-offer test (which doesn't generate a QR) stayed green; restored, confirmed unchanged. `npm run test:unit` 36/36 unaffected. `npm run lint` clean. **Real, named cost, not hidden**: fire-and-forget means a booking/cancellation can succeed with zero email ever sent and nothing retries it — see D-54, which also states plainly that `mail/mailer.js` violates CLAUDE.md's "emails go through outbox_events" invariant on purpose. | |
| P4-8 | ⭐ `cancelBooking()`: refund + seat release, routing freed seats through `bookings.service.js`/`offers.service.js` per §7.2 — **redefined to cancellation only**; booking history/detail/PDF split out to P4-9 below | ✅ **cancellation half only** — the `OFFER_RESERVED` routing itself is Phase 5's P5-3 | `server/src/modules/bookings/{bookings.queries,bookings.service,bookings.controller,bookings.routes}.js`, `server/tests/e2e/bookingCancel.test.js`, `docs/FILE_MANIFEST.md` | `npm run test:e2e` → 6 files, 16/16 (3 new). Happy path: refund `REFUNDED`, seat `AVAILABLE`/`booking_id` NULL, booking `CANCELLED`/`cancelled_at` set, response seats grouped by `categoryId`. Idempotent double-cancel: second call `cancelled:false`, exactly one `REFUNDED` payment row (composition with P4-1's `refund()`, no new code needed on either side). Non-owner → `403`, nothing changed. **Falsified live**: dropped `markBookingCancelled()`'s `AND status='CONFIRMED'` guard — the double-cancel test's second-call assertion correctly failed (`true` instead of `false`); restored, confirmed via `git diff` that only the intended new functions differ from HEAD. `assertTransition(BOOKED, AVAILABLE)` wired in as a third call site extending D-40/D-44 (see the new D-45). Freed seats returned as `Map<categoryId, seats[]>` — every group resolves to `AVAILABLE` today; the exact insertion point for P5-3's `OFFER_RESERVED` branch is named inline, citing D-7. `npm run lint` clean; full suite 31 unit + 16 e2e. | |
| P4-9 | Booking history, detail, PDF ticket | ⬜ **deferred to after Phase 5** — see ordering note above. Split from the original P4-8, which bundled this with cancellation | `bookings.*`, `ticketPdf.service.js` | E2E full lifecycle | |

**Exit criteria:** Book → email with a scannable QR lands. A rolled-back booking sends nothing.

---

## Phase 5 — Waitlist and time-limited offers ⭐

**Sequencing note (2026-08-24):** built immediately after P4-2/P4-8 (see Phase 4's ordering note
above), before the rest of Phase 4. One task at a time, each with its own approved plan — no
`/batch` (already excluded for this phase by that skill's own description, but restated here since
it's the scored phase this project cares most about getting right).

| ID | Change | Status | Files touched | Verified by | Commit |
|---|---|---|---|---|---|
| P5-1 | Join waitlist (only when the category has zero effective availability); unique per user/show/category | ✅ | `shared/schemas/waitlist.schema.js`, `shared/errors.js` (+`ALREADY_WAITLISTED`), `server/src/utils/errors.js` (+`AlreadyWaitlistedError`), `server/src/modules/seatmap/seatmap.queries.js` (exports `EFFECTIVE_STATE_CASE`), `server/src/modules/waitlist/{waitlist.routes,controller,service,queries}.js`, `server/src/app.js` (mounts `/api/v1/shows/:showId/waitlist`), `server/tests/e2e/waitlist.test.js`, `docs/FILE_MANIFEST.md` | `npm run test:e2e` → 7 files, 20/20 (4 new). Join on a zero-availability category → `201`, `position:1/total:1`; join on an available category → `422 VALIDATION_ERROR`, zero rows written; duplicate join by the same user → `409 ALREADY_WAITLISTED`, still exactly 1 row. **User-requested addition**: 2 users join, the first's entry is flipped to `CANCELLED` directly via SQL (no leave endpoint exists yet), a 3rd user joins and lands at position 2 of 2 — not 3 of 3 — proving `findQueuePosition()`'s `ROW_NUMBER()` is computed over the live `WAITING` set, closing the gap for free, rather than a stored counter that would need to be kept in sync. **Falsified live**: commented out the availability-check branch (`if (false && availableCount > 0)`), re-ran just `waitlist.test.js` — exactly the "rejects a join on an available category" test failed, the other 3 stayed green; restored, confirmed via `grep` no trace of the falsification remained. Real bug found and fixed mid-task, not just a config choice: `findQueuePosition()`'s `ROW_NUMBER()`/`COUNT(*) OVER()` came back as JS strings (`pg`'s default `bigint` handling), failing the position/total assertions until cast to `::int` (D-46). `npm run test:unit` 31/31 unaffected. `npm run lint` clean. | |
| P5-2 | Position via `ROW_NUMBER() OVER (ORDER BY enqueued_at)`; `GET /waitlist/me` | ✅ | `shared/schemas/waitlist.schema.js` (+`waitlistMeQuerySchema`), `server/src/modules/waitlist/{waitlist.queries,service,controller,routes}.js`, `server/tests/e2e/waitlist.test.js`, `docs/FILE_MANIFEST.md` | `npm run test:e2e` → 7 files, 24/24 (4 new). `GET /me` on a `WAITING` entry → `{status:'WAITING', position, total}`; no entry → `404 NOT_FOUND`; an entry flipped to `CANCELLED` via SQL → `{status:'CANCELLED', position:null, total:null}`. **The task's own exit criterion, proven directly** (not just re-proving P5-1's join-response renumbering): 2 users join, `GET /me` for user 2 confirms position 2 of 2, user 1 is cancelled, `GET /me` for user 2 again (not a fresh join) now reports position 1 of 1. **Falsified live**: removed the `status !== 'WAITING'` early-return branch (no optional chaining added), re-ran `waitlist.test.js` alone — exactly the "nulls position/total" test failed, with a `500`/`TypeError: Cannot read properties of null (reading 'position')`, not a silently wrong value; restored, confirmed via direct re-read that the file matches what was written. `npm run test:unit` 31/31 unaffected. `npm run lint` clean. Scope: `DELETE /shows/:id/waitlist` (leave) deliberately not built — it's in §9's endpoint list but has no BUILD_LOG task, and the user directed building only what a named task asks for. | |
| P5-3 | ⭐ Cancellation routes freed seats by category: empty queue → AVAILABLE, else → `OFFER_RESERVED` | ✅ | `server/src/modules/bookings/{bookings.service,bookings.queries}.js`, `server/src/modules/waitlist/{waitlist.queries,offers.queries,offers.service}.js` (2 new files), `server/tests/setup/fixtures.js` (+`offerTtlSeconds` param), `server/tests/e2e/bookingCancel.test.js`, `docs/FILE_MANIFEST.md` | `npm run test:e2e` → 7 files, 28/28 (4 new). Non-empty queue → seat `OFFER_RESERVED`, offer `PENDING`/`attempt_no:1`, entry `OFFERED`; **user-requested D-14 assertion**: `reserved_until` computed as `offerTtlSeconds * WAITLIST_MAX_CASCADE_ATTEMPTS + 60` from a known small `offerTtlSeconds`, asserted strictly greater than `expires_at` (`offerTtlSeconds` alone) — not just "both set." Multi-category booking resolves each category independently (one to `OFFER_RESERVED`, the sibling to `AVAILABLE`) in the same cancel call. **User-requested D-7 assertion**: a stranger's real `POST /holds` against the `OFFER_RESERVED` seat, before `reserved_until`, → `409 SEATS_UNAVAILABLE`, seat untouched — the exclusivity promise proven as a request, not a comment. **User-requested atomicity proof**: `vi.spyOn(offersQueries, 'insertWaitlistOffer').mockRejectedValueOnce(...)` forces a failure INSIDE the transaction, after the booking was already marked CANCELLED and the seat already flipped to OFFER_RESERVED in-memory — confirms via direct DB reads that the booking is still `CONFIRMED`, the seat still `BOOKED`, the payment still `CAPTURED`, no `waitlist_offers` row exists, and the entry is still `WAITING`. **Falsified live, beyond the mock-based proof**: temporarily split `cancelBooking()` into two separate `withTransaction()` calls (booking-cancel+refund in one, the per-category offer/release loop in a second) — the atomicity test's own booking-status assertion correctly failed (`'CANCELLED'` instead of `'CONFIRMED'`, since the first transaction had already committed independently of the second one's forced failure), proving the test genuinely depends on the single-transaction boundary rather than passing regardless; restored, confirmed via re-read that the function matches what was written. `npm run test:unit` 31/31 unaffected. `npm run lint` clean. Scope, all flagged in the plan and tracked in "Phase 5 debt" below: offer token is a placeholder (D-49); `job_queue`/`outbox_events`/`pg_notify` deferred (D-50, mirrors P3-5/P4-6/P4-7/P3-6's own already-deferred status) — correctness doesn't depend on any of them, since the offer's `expires_at`/`reserved_until` are already enforced by the existing `acquireSeats()` predicate. | |
| P5-4 | ⭐ Single-use HMAC offer token; only the hash stored; raw only in the email link | ✅ | `server/src/modules/waitlist/{offers.service,offers.queries}.js`, `server/tests/unit/offerToken.test.js`, `docs/FILE_MANIFEST.md` | `npm run test:unit` → 2 files, 36/36 (5 new). `generateOfferToken()` matches §7.3's literal shape (`raw = offerId.randomBytes`, `tokenHash = sha256(raw).hex`) — asserted directly against an independently-computed hash, not just "some hash exists." Determinism (same raw → same hash twice), tamper detection (a modified raw → a different hash), and 20,000-token generation with zero raw or hash collisions. **Falsified live**: replaced the real hash with a constant — 3 of 5 tests correctly failed (the hash-matches-independent-computation test, the determinism test, the collision test); the tamper test happened to still pass under that specific break (any two independently-wrong hashes are still unequal), so falsified again targeting it directly — hashed only `offerId` (ignoring the random suffix) — and it STILL passed, for the same reason: a "not equal" assertion between two real (if wrong) hashes can't distinguish "correct" from "differently wrong." Documented as an inherent limit of that one assertion rather than a gap — the determinism test is what actually pins down correctness, and it was already proven to fail under a broken implementation. Restored both times, confirmed via re-read. Real, non-obvious design calls made and logged: D-51 (offer `id` generated client-side so it can be embedded in the raw token before the row exists — `insertWaitlistOffer()`'s signature grew an explicit `id` param) and D-52 (plain SHA-256, not a keyed HMAC, despite §7.3's own section heading — the random bytes ARE the secret, there's no second party's key to authenticate against). `npm run test:e2e` 28/28 unaffected (the real token slots into P5-3's existing `waitlist_offers` assertions with no changes needed). `npm run lint` clean. Resolves D-49 and its "Phase 5 debt" bullet. | |
| P5-5 | ⭐ `GET /waitlist/offers/:token` + `POST .../accept` — single-use, `OFFER_RESERVED → BOOKED` reusing P4-2's guarded-transaction shape with `fromState` genuinely parameterised (D-40) | ✅ **revived** — see the note below | `shared/errors.js` (+`OFFER_INVALID`/`OFFER_EXPIRED`), `server/src/utils/errors.js` (+`OfferInvalidError`/`OfferExpiredError`, both 410), `server/src/modules/waitlist/{offers.queries,offers.service}.js` (+`findOfferByTokenHash`, `findOfferSeatsByIds`, `confirmOfferSeats`, `markOfferAccepted`, `getOfferByToken`, `acceptOffer`), `server/src/modules/waitlist/waitlist.queries.js` (+`findEntryById`, `markEntryConverted`), `server/src/modules/waitlist/{offers.controller,offers.routes}.js` (2 new files), `server/src/modules/bookings/bookings.service.js` (`generatePlaceholderReference`/`generatePlaceholderQrToken` exported for reuse), `server/src/app.js` (mounts `/api/v1/waitlist/offers`, and `/waitlist/claim/:token` → `client/index.html`), `client/index.html` (claim view + boot-time routing) | Live over real HTTP end-to-end, twice, against the dev DB (not the test harness — no automated test written, see below): a 1-seat venue built via the real admin/organiser endpoints, customer A books it, customer B waitlists, A cancels → seat `OFFER_RESERVED`, offer email fires. Claim link extracted from the real Ethereal preview page (not faked). `GET /waitlist/offers/:token` → seats/price/`secondsRemaining`. `POST .../accept` → `200` with a real booking whose `userId` is B's (the offeree), not any caller's — proving the public, token-authenticated design actually works, since there is no `req.user` on this route at all. DB re-read directly: seat `BOOKED` under the new booking, offer `ACCEPTED`, entry `CONVERTED`. A second accept (sequential) → `410 OFFER_INVALID`; `GET` after accept → `410 OFFER_INVALID` too. **The real concurrency proof, matching this project's own bar for atomic guards (P3-2/P3-3's style)**: a second, independent 2-seat scenario, then two genuinely concurrent `POST .../accept` calls (`Promise.all`) at the SAME fresh token — exactly one `200` (with both seats, correct `totalCents`), one `410 OFFER_INVALID`, proving `confirmOfferSeats()`'s `state = 'OFFER_RESERVED' AND expires_at > now()` predicate is the actual single-use boundary, not just the offer's own `status` field (which only gates the sequential-retry case, since it's checked before the atomic step). No explicit falsification pass this time (predicate temporarily stripped, re-run, restored) — skipped under the remaining time budget; the concurrent-race result itself (exactly 1-of-2 winners under genuine `Promise.all` contention) is the same signature P3-2/P3-3 treat as proof, just without the additional break-and-restore step. `npm run test:unit` 36/36, `npm run test:e2e` 30/30 (8 files) unaffected — no new automated test added for this endpoint pair either, same time-boxing tradeoff as P7-1/P8-1. `npm run lint` clean. | |
| P5-6 | ❌ Dropped (user directive, 2026-08-24) — see the note below | ⬜ | `offers.service.js`, `queue/handlers/offerExpiry.js` | — | |
| P5-7 | ❌ Dropped (user directive, 2026-08-24) — see the note below | ⬜ | `jobs/offerReconciler.job.js` | — | |
| P5-8 | ❌ Dropped (user directive, 2026-08-24) — see the note below | ⬜ | `tests/e2e/waitlist.test.js` | — | |

**Scope decision (2026-08-24, user directive — revised same day to stop one task earlier than
first decided):** Phase 5 stopped after P5-4 for one session. P5-6 (cascade), P5-7 (offer
reconciler cron), and P5-8 (the 10-way offer race test) are still dropped, not deferred — no later
task in the ledger picks these back up. **P5-5 was revived in a later session** (same day, ~30-
minute budget, "before deploy") — the accept/claim half was NOT unreachable-by-design after all;
see its own row above.

**Cascade-on-lapse is designed, not wired**: §7.4's mechanism is fully specified in
`docs/PROJECT_PROMPT.md`, and its one reusable building block —
`waitlist.queries.js#claimNextWaitingEntry()`, the `FOR UPDATE SKIP LOCKED` head-of-queue query —
already exists and is already exercised in production code (P5-3's `cancelBooking()` dispatch).
What's missing is the trigger: an `OFFER_EXPIRY` job handler and its cron/poller wiring (P5-6/P5-7
was where that would have landed) that calls `claimNextWaitingEntry()` again when an offer lapses,
mints a new token via P5-4's real token module, and either re-offers (`attempt_no + 1`, same
`OFFER_RESERVED` seats, `reserved_until` left untouched per D-14) or releases to `AVAILABLE` once
`MAX_CASCADE` is reached or the queue drains. Without it, an offer that lapses stays
`OFFER_RESERVED` until `reserved_until` passes (Layer 1 still applies — a public hold can reclaim
the seat once that fixed outer bound is reached, same as any other lazy-expiry case), but nobody
actively gets offered it in between. §6.6's "10 waitlisted users racing one offer" proof scenario
is dropped along with P5-8, not owed to a future task.

**Exit criteria — partially met:** §7's own exit criterion ("cancelling a booking gives the next
person in line a working, expiring, single-use claim link that cascades correctly on lapse") named
two halves. The FIRST half is now fully built and proven: waitlist membership and live position
(P5-1/P5-2), the atomic single-transaction cancel → offer dispatch to a real `OFFER_RESERVED` seat
with a real single-use token (P5-3/P5-4), and — as of P5-5 — a working claim link that resolves,
shows the offer, and converts it to a real booking for the correct (offeree, not caller) user,
single-use under a genuine concurrent-accept race. Only the SECOND half — "cascades correctly on
lapse" — remains the scope cut: an offer that lapses without being accepted just sits
`OFFER_RESERVED` until `reserved_until` (Layer 1 still applies), with nobody actively re-offering
it to the next person in line. See "Cascade-on-lapse is designed, not wired" above, still accurate.

---

## Phase 6 — Realtime seat map

| ID | Change | Status | Files touched | Verified by | Commit |
|---|---|---|---|---|---|
| P6-1 | Socket.IO server with JWT handshake auth (no adapter — single instance, documented as the scaling seam) | ⬜ | `realtime/io.js` | Two browsers, one server, both receive | |
| P6-2 | Room helpers + typed emit helpers used by every service | ⬜ | `realtime/rooms.js`, `emit.js` | No raw event strings anywhere | |
| P6-3 | Wire all seven events through `pg_notify` → listener → room. Services never emit directly | ⬜ | services + `notify/*` | Two-browser manual check; no direct `io.emit` in any service | |
| P6-4 | Presence counting, throttled 2s | ⬜ | `realtime/presence.js` | Count correct after abrupt disconnect | |
| P6-5 | Reconnect reconciliation: client hard-refetches the seat map | ⬜ | `client/src/lib/socket.js`, `hooks/useSeatMap.js` | Kill network 30s → state converges | |

**Exit criteria:** Seat changes propagate between two browsers within a second and survive a disconnect.

---

## Phase 7 — Static client (vanilla, no build step)

**Scope decision (2026-08-24, user directive — Decisions Ledger D-53):** the original 10-task
React/Vite/Router/TanStack Query/Zustand/Tailwind plan below is dropped entirely, not deferred or
scoped down within the same framework. Replaced with one static `client/index.html` — vanilla JS,
no framework, no bundler, no build step — served by the Express server itself at `/`. `CLAUDE.md`'s
Stack section already reflects this. The 10 original rows are kept in the table, struck through,
so the historical record of what was originally planned survives; P7-1 is repurposed for the
actual deliverable rather than left as dead weight alongside a purely additive new row.

| ID | Change | Status | Files touched | Verified by | Commit |
|---|---|---|---|---|---|
| P7-1 | ~~Vite + Tailwind + router shell~~ **Repurposed**: `client/index.html` — one vanilla file, no build step, served statically by Express at `/`. Login/register, event browse, seat map (visual grid from `grid_row`/`grid_col`, per-seat state, click-to-select up to 6), hold with a live mm:ss countdown, confirm-to-QR, and a cancel button. | ✅ | `client/index.html`, `server/src/app.js` (static mount + CSP relaxation), `package.json` (root — `client` dropped from `workspaces`), `client/package.json` (deleted — no longer a workspace), `server/src/config/env.js`/`.env.example`/`.env` (`WEB_URL` now the server's own origin, not `:5173`), `server/src/db/seed.js` (grown: one demo venue + two published events/shows so the client has something to browse — see below), `server/src/modules/shows/{shows.queries,shows.service,shows.controller,shows.routes}.js` (+`listShowsByEvent`, `GET /events/:eventId/shows` — the client's event-detail screen needs a showId and nothing else in the API lists shows by event), `server/src/modules/bookings/{bookings.service,bookings.controller,bookings.routes}.js` (+`GET /bookings/:id/ticket`, PNG QR reusing P4-2's placeholder `qrToken` and the same `qrcode` render mail/mailer.js already uses — not the real signed-JWT P4-5 mechanism, still open Phase 4 debt) | Full flow proven live over real HTTP against the dev DB (not the test harness — this is a client-integration task, not a new server mechanism): login → `GET /events` → `GET /events/:id/shows` → `GET /shows/:id/seatmap` → `POST /holds` (2 seats) → `POST /bookings/confirm` → `GET /bookings/:id/ticket` (returned a genuine 320×320 PNG, `file` confirmed) → `POST /bookings/:id/cancel` (seats back to `AVAILABLE`) → confirmation email fired (Ethereal preview URL in the server log). Root path `/` serves `client/index.html` (curl confirmed 200 + the file's own `<title>`) after fixing an off-by-one in the static-mount path (`../../../client` resolved above the repo root — `../../client` is correct from `server/src/app.js`). Inline script extracted and run through `node --check` — no syntax errors. Full suite unaffected: `npm run test:unit` 36/36, `npm run test:e2e` 30/30 (8 files), `npm run lint` clean. No new automated test written for the 3 new backend endpoints (`GET /events/:eventId/shows`, `GET /bookings/:id/ticket`, `GET /events/:id/summary`) — none of the four scored hard mechanisms, and time-boxed against the ~6h deadline the same way Phase 0-2 shipped on live-proof alone before Vitest existed; tracked as open follow-up, not silently skipped. | |
| P7-2 | ❌ Dropped (D-53) — ~~Auth pages + `ProtectedRoute` by role~~ | ⬜ | — | — | |
| P7-3 | ❌ Dropped (D-53) — ~~Event browse + detail + showtime picker~~ | ⬜ | — | — | |
| P7-4 | ❌ Dropped (D-53) — ~~Seat map component (React)~~ — folded into P7-1 above, vanilla instead | ⬜ | — | — | |
| P7-5 | ❌ Dropped (D-53) — ~~Optimistic selection + rollback on 409~~ | ⬜ | — | — | |
| P7-6 | ❌ Dropped (D-53) — ~~Checkout with live countdown~~ | ⬜ | — | — | |
| P7-7 | ❌ Dropped (D-53) — ~~Success screen, booking history, cancel dialog~~ | ⬜ | — | — | |
| P7-8 | ❌ Dropped (D-53) — ~~Sold-out waitlist card~~ | ⬜ | — | — | |
| P7-9 | ❌ Dropped (D-53) — ~~Claim page~~ — moot regardless, since P5-5 (the accept endpoint it would call) is also dropped | ⬜ | — | — | |
| P7-10 | ❌ Dropped (D-53) — ~~Skeletons, empty states, error boundaries~~ | ⬜ | — | — | |

**Exit criteria (revised):** the seat map is visible and live at `/` with no build step and no
separate client process — the scored line item D-53 names, not a full multi-screen SPA experience.

---

## Phase 8 — Reports, admin, check-in

| ID | Change | Status | Files touched | Verified by | Commit |
|---|---|---|---|---|---|
| P8-1 | Organiser summary: revenue, tickets sold, occupancy %, waitlist depth, per category | 🟨 **partial — no waitlist depth** | `server/src/modules/events/{events.queries,events.service,events.controller,events.routes}.js` (+`getEventSummaryByCategory`, `getEventSummary`, `GET /events/:id/summary` — ORGANISER + `requireOwnership`, reusing `loadEventForOwnership`) | Live over real HTTP: seeded event, 2 categories × 16 seats each — summary correctly reported `totalSeats:32, sold:0` before any booking; after a hold→confirm→cancel cycle on 2 seats, summary correctly went back to `sold:0` (revenue only counts `CONFIRMED` bookings, not cancelled ones — the query's `LEFT JOIN ... WHERE status = 'CONFIRMED'` subquery). A `CUSTOMER` calling the same route → `403 FORBIDDEN`. Built in a dedicated `events.queries.js` function, not a `modules/reports/*` file as the row originally sketched — one event's own summary didn't need a new module. Waitlist depth (the row's own 4th metric) is NOT included: the waitlist module has no per-show/per-category aggregate read today, only per-user position (`waitlist.queries.js#findQueuePosition`) — building one wasn't worth the time against the ~6h deadline's higher-priority items (client, email). No automated test — see P7-1's row for why. | |
| P8-2 | Revenue chart + CSV export | ⬜ | `components/RevenueChart.jsx` | CSV opens cleanly | |
| P8-3 | Attendee manifest per show, paginated and searchable | ⬜ | `reports.*` | E2E | |
| P8-4 | Admin venue designer: visual grid, assign categories to rows | ⬜ | `pages/admin/*` | Create a 200-seat venue in the UI | |
| P8-5 | ⭐ Check-in: `POST /tickets/verify` + camera scanner page; second scan → `ALREADY_USED` | ⬜ | `modules/tickets/*`, `pages/Scan.jsx` | Scan a real emailed QR with a phone | |

**Exit criteria:** An organiser sees money and can scan a real ticket at a real gate.

---

## Phase 9 — Hardening and proof

| ID | Change | Status | Files touched | Verified by | Commit |
|---|---|---|---|---|---|
| P9-1 | Rate limiting (holds 10/min/user, auth 5/min/IP) | ⬜ | `middleware/rateLimit.js` | 429 returned correctly | |
| P9-2 | Audit logging on every state-changing action | ⬜ | service layer | Rows present after a full flow | |
| P9-3 | Correlation IDs in `pino`; `/health` reports pool stats, `job_queue` pending/dead, outbox backlog, listener state | ⬜ | `utils/logger.js`, `health.routes.js` | Trace one request end to end | |
| P9-4 | Graceful shutdown: drain requests, stop the poller mid-batch safely, close the `LISTEN` client and the pool | ⬜ | `server/src/index.js` | SIGTERM mid-hold → no corruption, no `RUNNING` jobs stranded | |
| P9-5 | k6 load test: 200 VUs on one show; capture the p95 chart for the README | ⬜ | `tests/load/seatRush.js` | Zero double-bookings under load | |
| P9-6 | Coverage ≥80% on `holds`, `bookings`, `waitlist` modules | ⬜ | — | Coverage report in CI | |

**Exit criteria:** The system stays correct under load, and you have a chart that proves it.

---

## Phase 10 — Docs, deploy, demo

| ID | Change | Status | Files touched | Verified by | Commit |
|---|---|---|---|---|---|
| P10-1 | ⭐ `docs/SYSTEM_DESIGN.md` — ≤800 words, leading with lazy expiry and the READ COMMITTED argument | ⬜ | `docs/SYSTEM_DESIGN.md` | Word count verified | |
| P10-2 | `README.md`: quickstart, architecture diagram, env table, mechanism explanations, **and the "Why there is no Redis" argument** | ⬜ | `README.md` | Clean machine running in under 5 min without Docker | |
| P10-3 | ⭐ `docs/SEQUENCE_DIAGRAMS.md` — Mermaid for hold→book, TTL expiry, cancel→cascade | ⬜ | `docs/` | Renders on GitHub | |
| P10-4 | `docs/API.md` + `DB_SCHEMA.md` (Mermaid ERD, index rationale) | ⬜ | `docs/` | Matches live Swagger | |
| P10-5 | ⭐ `/demo` control panel: simulate 50 concurrent holds, force-expire, trigger cascade | ⬜ | `client/src/pages/Demo.jsx` | Each button demonstrably fires | |
| P10-6 | Deploy API + managed Postgres (Render/Railway) and client (Vercel); migrate, seed, warm. No Dockerfile — platforms build from `package.json` | 🟨 **prep complete, deploy pending user account creation** | `render.yaml`, `docs/DEPLOYMENT.md` (+"Production deployment (Render)" section) | Deploy-ready side verified: `render.yaml` parses (`js-yaml`, same tool D-20 used for CI), `startCommand`'s migrate→seed→start chain traced against the real scripts it calls (`migrate.js`'s own `process.exit(0)` on success, `seed.js`'s clean exit after `pool.end()` — both let the `&&` chain continue). One service, not two — D-53 already made the client and API one Express process, so no separate client host is needed the way the original React/Vite plan (and this row's own original text) assumed. Actual account creation, GitHub connection, and the Blueprint "New +" click are steps only the user's browser session can do — see `docs/DEPLOYMENT.md` for the exact steps; not run in this session. | |
| P10-7 | `DEMO.md` grader script + credentials; source zip (no `node_modules`, no `.env`) | ⬜ | `DEMO.md` | Zip extracts and runs clean | |

**Exit criteria:** Someone who has never seen the project goes from a link to impressed in five minutes.

---

## Decisions Ledger

Record every non-obvious choice here as it is made. This is the artefact that shows judgement.

| # | Date | Decision | Alternatives considered | Rationale |
|---|---|---|---|---|
| D-1 | — | Raw SQL with `pg`, no ORM | Prisma, Sequelize, Knex | The atomic acquire must be hand-written SQL regardless. An ORM would hide the one query that matters and add a layer to explain in review. |
| D-2 | — | Lazy expiry: `expires_at` in the SQL predicate is authoritative | Cron-only release; TTL held only in a cache | Correctness must not depend on a worker running. Schedulers materialise and broadcast; they don't decide. **READ COMMITTED verification (for P10-1):** confirmed against Postgres semantics — under READ COMMITTED, a statement that blocks on a row lock re-evaluates its WHERE clause against the latest *committed* row version once the lock is released (the EvalPlanQual mechanism), so the loser of a race simply fails to match; no retry loop, no `SERIALIZABLE`, no lost update. In `holds.queries.js` specifically, the `FOR UPDATE` CTE locks unconditionally on `show_id`/`seat_id`, and the state predicate lives in the outer `UPDATE`, so by the time it evaluates it is always reading post-lock-wait, current data — the CTE's data-modifying nature guarantees it runs to completion (all waiting included) before the outer statement consumes it. One residual nuance: `ORDER BY seat_id` in the CTE prevents acquire-vs-acquire deadlocks (D-4), but the Layer-3 reconciler's bulk sweep has no such ordering, so an acquire's expired-hold reclaim can, rarely, deadlock against a concurrent reconciler pass — Postgres detects it and aborts one side (`40P01`). Not a correctness gap (the reconciler is idempotent and re-runs in 30s regardless), but `holds.service.js` should catch `40P01` and retry its transaction once rather than surfacing it as a `409`. |
| D-3 | — | READ COMMITTED + `FOR UPDATE` CTE instead of SERIALIZABLE | SERIALIZABLE with retry loop; optimistic version column | Postgres re-evaluates the `WHERE` clause after a lock wait, so the loser simply doesn't match. No retries, no serialization failures. |
| D-4 | — | Lock ordering by `seat_id` | Unordered `ANY($2)` | Two overlapping multi-seat requests take locks in the same order, so they queue rather than deadlock. |
| D-5 | — | Every query function takes an explicit `client` | Module-level `pool.query` | With `pg`, calling `pool.query` inside a transaction gets a *different* connection and silently escapes it. Passing the client makes that impossible. |
| D-6 | — | **No Redis at all.** Postgres is the only datastore | Redis for locks, delayed jobs, keyspace TTL, queue position | Every job Redis would do has a Postgres primitive that is *transactionally consistent with the booking data itself*. One datastore = one source of truth, one transaction boundary, no cross-system consistency problem. The scaling threshold where Redis becomes right is a second API instance — say so rather than pretending it never applies. |
| D-6a | — | No application-level lock on seat selection | Redlock; advisory locks | The `FOR UPDATE` row lock is already the correctness boundary. A lock elsewhere could only agree with it or be wrong, and adds a failure mode. |
| D-6b | — | `job_queue` table + `FOR UPDATE SKIP LOCKED` instead of BullMQ | BullMQ; `pg_cron`; setTimeout | Same claim-exactly-once semantics, ~80 lines, and it reuses the identical primitive as the waitlist cascade — one pattern to learn and defend instead of two. Jobs enqueue *inside* the caller's transaction, so a hold cannot exist without its expiry job. |
| D-6c | — | `pg_notify` inside the transaction instead of emitting from services | Direct `io.emit` after commit; Redis pub/sub | NOTIFY only delivers if the transaction commits, so a broadcast can never announce a seat release that then rolls back. Strictly stronger than the Redis version. |
| D-7 | — | Cancelled seats go to `OFFER_RESERVED`, never back to the public pool | Release then re-check | Otherwise a random browser snipes the seat the waitlist was promised. |
| D-8 | — | Transactional outbox for all email | Fire-and-forget after commit | Prevents emails for rolled-back bookings and lost emails on crash. |
| D-9 | — | `MAX_CASCADE_ATTEMPTS = 5` | Unbounded cascade | Bounds how long a seat sits locked behind an inactive queue. |
| D-10 | — | QR signed with a secret distinct from auth | Reuse `JWT_ACCESS_SECRET` | Blast-radius isolation; a leaked ticket key can't mint sessions. |
| D-11 | — | Real local `ticket_booking_test` database over mocks for concurrency tests | `vi.mock` on the pool; testcontainers | A mocked database has no row locks, so a mocked concurrency test proves nothing. Containers were unavailable on the dev machine, so tests use the real Postgres already installed — same guarantee, no dependency. |
| D-12 | — | JSDoc + `checkJs` instead of TypeScript | Full TS | Keeps the stack familiar while still catching shape errors in the editor, and doubles as the documentation the reviewer wants. |
| D-13 | 2026-08-23 | Fixed `.gitignore`, which listed `CLAUDE.md`/`BUILD_LOG.md`/`FILE_MANIFEST.md`/`PROJECT_PROMPT.md` by bare filename and so was silently excluding the spec docs (including their `docs/` copies) from every commit; relocated `FILE_MANIFEST.md` from repo root to `docs/FILE_MANIFEST.md` to match where CLAUDE.md and the manifest itself say it lives | Leave as found | Both working rule #3 ("BUILD_LOG.md and FILE_MANIFEST.md updates go in the same commit as the code") and every task's Definition of Done depend on these files actually being tracked. Caught before it silently broke every future task close-out. |
| D-14 | 2026-08-23 | Two timestamps on `show_seats` for `OFFER_RESERVED`: `expires_at` (the current cascade attempt's deadline, extended on each cascade) and `reserved_until` (the fixed end of the whole cascade window, set once on entry, never extended). `reserved_until = now() + (offer_ttl_seconds × WAITLIST_MAX_CASCADE_ATTEMPTS) + 60s grace`. Acquire predicate for `OFFER_RESERVED` now checks `reserved_until <= now()`, not `expires_at` | A single `expires_at` reused for both `HELD` and `OFFER_RESERVED` — the original §6.1 predicate, which let a public hold reclaim a seat mid-cascade the instant one attempt's `expires_at` lapsed, contradicting §7.2/D-7 | Resolves the contradiction flagged when critiquing the spec before P0-1. Attempt N of a cascade ends by `N × offer_ttl_seconds`, so `MAX_CASCADE × offer_ttl_seconds` is a hard upper bound on how long any *legitimate* cascade can run. Past `reserved_until`, no legitimate cascade can still be in flight, so the seat is definitively public — a public hold can acquire it. Before `reserved_until`, it can't, no matter how stale the current `expires_at` looks, so offer exclusivity (D-7) holds even mid-cascade. Lazy expiry (D-2) still backstops `reserved_until` itself — no worker needs to run for the seat to become correctly acquirable once it passes. |
| D-15 | 2026-08-23 | ESLint config is `eslint.config.js` (flat config), not the `.eslintrc.json` named in the original P0-2 row and `FILE_MANIFEST.md` | `.eslintrc.json` (legacy format) | The row already said "ESLint flat config" — a self-contradiction, since flat config is only recognised under the filename `eslint.config.js`. ESLint 9+ (10.9.0 installed) uses flat config by default and dropped `.eslintrc*` support entirely, so the legacy filename would have silently produced a no-op lint config. `jsdoc` rules scoped to `server/src/**` and `shared/**` only, matching CLAUDE.md's commenting standard, which targets those modules specifically. |
| D-16 | 2026-08-23 | Verified `postgresql.conf` tuning (P0-3) by reading the file directly on disk instead of via `psql`; edited `max_connections` on disk but did not restart the Windows service myself | Ask for the `postgres` password in chat and run live `SHOW`/`psql -l` checks | The config file is readable/writable without credentials, which covers the literal task wording ("verify ... in `postgresql.conf`"). Restarting `postgresql-x64-16` needs an elevated session the coding shell doesn't have — `Restart-Service` failed with a permissions error rather than silently no-op'ing, so this is a visible, reportable blocker, not a swallowed one. Asking the user to paste a database password into chat for a check that's easily done from their own elevated terminal wasn't worth the exposure, especially since the databases' existence was already asserted in the Environment status table above. Net effect: `max_connections=50` is correct on disk but **not yet live** — flagged explicitly in the P0-3 row rather than marked verified. |
| D-17 | 2026-08-23 | P0-4 created three files beyond its stated two (`server/src/app.js`, `index.js`): `shared/errors.js` (minimal, 2 codes), `server/src/middleware/errorHandler.js`, `server/src/modules/health/health.routes.js` | A stub error handler with an inlined string, or no error handler at all until a later task | CLAUDE.md is explicit that error strings are never inlined and that error codes live in `shared/errors.js`; an error-handler middleware with nothing to import would have to violate one of those or invent domain-specific codes (`SEATS_UNAVAILABLE`, ...) that belong to Phase 3/4/5. Kept both new files to the minimum a bare skeleton needs — 2 generic error codes, a 500-fallback-only handler — so later phases extend them rather than rewrite them. `health.routes.js` similarly split out now (liveness only) rather than inlining `/health` in `app.js`, since the manifest already names it as its own file and P9-3 will extend this same file rather than refactor a route out of `app.js` later. |
| D-18 | 2026-08-23 | P0-5: (a) secrets and `DATABASE_URL` are required with no default; `NODE_ENV`/`PORT`/`API_URL`/`WEB_URL` get sensible dev defaults instead, even though §12's var list doesn't mark either category explicitly. (b) `.env` loaded via Node 22's built-in `process.loadEnvFile()` instead of adding the `dotenv` package. (c) Installed Zod resolved to v4.4.3 — used the v4 idioms (`z.url()` top-level, not the deprecated `z.string().url()` chain; `ctx.addIssue({ code: 'custom', ... })`, not the `ZodIssueCode` enum) rather than writing v3-style code that happened to still parse | Treat every var in §12 as required (matching "fail fast... never start half-configured" literally); add `dotenv` as a dependency; guess at Zod v3 API without checking what actually installed | (a) A default secret or a default `DATABASE_URL` is a real vulnerability/footgun — "fail fast" is about catching *wrong* config, not about refusing to have any reasonable defaults at all; `PORT`/`WEB_URL` etc. already had ad-hoc fallbacks hardcoded in P0-4's `app.js`/`index.js` that this task was always going to formalize into `env.js`, not remove. (b) One fewer dependency for a job Node now does natively (stable since Node 20.6, and this project targets 22.19) — checked it actually works (guarded with `existsSync` since it throws `ENOENT` on a missing file, which is the expected case in CI/production). (c) `npm install zod` was never going to pin to v3 just because the spec was written against it; checked the installed version's real API with small probe scripts before writing `env.js` against it; `z.prettifyError()` (new in v4) is what makes the boot-failure report actually readable instead of a raw JSON issue dump. |
| D-19 | 2026-08-23 | P0-6: two things fixed after live-testing rather than trusting config on faith. (a) `swagger.js`'s glob pattern is forced to forward slashes (`.split(path.sep).join('/')`) after `path.join()`. (b) No CSP override added for `/api/docs`, despite that being a well-known helmet + swagger-ui gotcha | (a) Leave the `path.join()` output as-is | (a) On Windows, `path.join()` produces backslash-separated paths, but the glob library `swagger-jsdoc` uses internally treats backslash as a glob escape character, not a separator — so the pattern silently matched **zero files**. No error, no warning, just an empty `paths: {}` in the generated spec, which reads exactly like "there are no documented routes yet" instead of "the glob is broken." Caught only by actually reading the JSON spec's `paths` key after boot, not by the page returning 200 (it always did). (b) The standard fix for that gotcha is disabling or relaxing CSP for the docs route — started down that path, then tested the actual rendered HTML first: this version of `swagger-ui-express` writes its init config to a same-origin `swagger-ui-init.js` file loaded via `<script src>`, not an inline `<script>` block, so `script-src 'self'` (helmet's default, no relaxation) already covers it, and the one inline `style=""` attribute in the page shell falls back to `style-src`, which already carries `'unsafe-inline'` in helmet's default. Added the override, tested, found it unnecessary, removed it — shipping an unverified CSP relaxation "just in case" would have widened the attack surface for a problem that doesn't exist in this version. |
| D-20 | 2026-08-23 | P0-7: `DATABASE_URL` and the three JWT/QR secrets in `ci.yml` are plain inline fake values, not GitHub repository Secrets | Store them as GitHub Secrets, referenced via `${{ secrets.* }}` | Secrets configured on this specific repo do not carry over to a fork, and a grader is expected to clone or fork the project to run it. If real config lived only in this repo's Secrets settings, CI would go red for anyone but the original owner — through no fault of their own, and directly undercutting "visible green CI is part of the pitch" (`docs/FILE_MANIFEST.md`'s own framing of this file). The Postgres container these values point at exists only for the few minutes the job runs and is destroyed after; nothing here has a genuine confidentiality need, so plain YAML is the correct tool, not a shortcut. Real production secrets (P10-6's deploy) are a different situation — those *should* be platform-managed secrets, because a production `DATABASE_URL` staying confidential is the whole point. |
| D-21 | 2026-08-23 | Exit criteria (and any "Verified by" cell) must name a command that actually exists and was actually run — never a command that's true "in spirit" | Leave the Phase 0 exit criterion as `npm run dev:server` while no such script existed, on the reasoning that `node server/src/index.js` proved the same thing | Caught during the `/close-phase 0` audit: the literal exit criterion (`npm run dev:server`) had never been added to any `package.json`, despite the API demonstrably booting via a different, unwritten-down command. No task's file list ever named adding it, so it fell through a gap between P0-4 (built the app) and P0-7 (assumed the whole phase's exit bar was already covered). A grader — or a future session — runs the literal command in the doc, not the spirit of it; "it boots some other way" is not evidence the documented path works. Fixed by adding `dev:server` to root `package.json` (delegating to a new `server` workspace `dev` script, `node --watch src/index.js`) and verifying it live from a clean process. The same risk exists for `dev:client` — flagged explicitly under P7-1 rather than left implicit, so it doesn't repeat. |
| D-22 | 2026-08-23 | `server/src/db/migrate.js` calls node-pg-migrate's programmatic `runner()` API directly, instead of the `db:migrate` scripts shelling out to the `node-pg-migrate` CLI | Shell out to the CLI with `--envPath ../.env`, matching how the CLI is normally used | Discovered live, mid-batch, that the CLI's `--envPath` flag silently does nothing without the `dotenv` npm package installed: it wraps the load in `await tryImport("dotenv")` and just skips it — no warning, no error — if that import fails (`node_modules/node-pg-migrate/bin/node-pg-migrate.js`). This project deliberately has no `dotenv` dependency (D-18: Node's built-in `process.loadEnvFile()` covers the same job). Adding `dotenv` back just to satisfy one CLI flag's undocumented assumption would have meant two different env-loading mechanisms in the same codebase. The programmatic `runner()` API has no such dependency — the wrapper reuses the exact same validated `env` object the server already boots from. |
| D-23 | 2026-08-23 | `server/src/config/env.js`'s `SMTP_PORT` schema wraps `z.coerce.number()` in a `z.preprocess()` step that treats `""` as `undefined` | Leave `z.coerce.number().int().positive().optional()` as originally written | Discovered live running the first real migration against a real `.env`: `SMTP_PORT=` (an empty-but-present line — exactly what `.env.example` ships, deliberately, so SMTP stays blank in dev per P4-7's Ethereal auto-account) crashed boot. `z.coerce.number()` coerces via JS's `Number('')`, which is `0`, not `NaN` — so the empty string didn't fail as "not a number," it failed `.positive()` as "the number zero," a much more confusing error to debug blind. Any optional numeric env var that can legitimately be blank needs this same preprocess step, not just `SMTP_PORT`. |
| D-24 | 2026-08-23 | `eslint.config.js`'s `jsdoc/require-jsdoc` rule now sets `require: { FunctionDeclaration: false }` alongside `contexts` | Trust `contexts` alone to scope the rule to exported functions, as originally configured at P0-2 | Discovered live at P1-4: `server/src/queue/poller.js`'s internal, unexported `completeJob()`/`failJob()` helpers were flagged despite matching neither of the `contexts` entries. `contexts` *adds* extra trigger points on top of the rule's `require` object, which defaults `FunctionDeclaration` to `true` for every function declaration regardless of export status — it does not replace that default. P0-2's original comment claiming `contexts` alone scoped the rule to exported functions was wrong; it happened not to matter until this batch because no undocumented internal `function` declaration had existed in the codebase before now. |
| D-25 | 2026-08-24 | `signRefreshToken` includes an explicit `jti: crypto.randomUUID()` claim | Trust `jsonwebtoken`'s default claims (`sub`, `familyId`, `iat`, `exp`) to make every token unique | Discovered live running the reuse-detection proof: two refresh tokens issued for the same user within the same wall-clock second are byte-for-byte identical JWTs — `iat` has only 1-second resolution, and nothing else in the payload changes between calls. Identical tokens hash identically, which crashed the request on `refresh_tokens.token_hash`'s UNIQUE constraint. Registering and then immediately refreshing (no network delay between the two calls) reproduced it every time; an earlier HTTP-based smoke test had happened to work only because manual `curl` round-trips left enough of a gap. This is a real concurrency risk (two tabs logging in near-simultaneously), not a test-only artifact. |
| D-26 | 2026-08-24 | `refreshTokens()`'s reuse-detection branch returns a result object from inside `withTransaction()` and only decides whether to throw `RefreshInvalidError` *after* the transaction has committed, instead of throwing directly from inside the branch that calls `revokeRefreshTokenFamily` | Throw `RefreshInvalidError` immediately after calling `revokeRefreshTokenFamily`, inside the same transaction | The single most important bug this batch found: `withTransaction()` rolls back on **any** thrown error, unconditionally — that's the whole point of it (see its own header). Throwing to report "reuse detected" from inside the same transaction that just revoked the token family rolled that revocation back along with everything else, so the family was never actually revoked — the very next request with the supposedly-dead token succeeded normally. Caught only because the proof script checked the database's actual state after each step instead of trusting that a thrown error meant the intended side effect had happened. The general lesson, not just this one call site: a transaction whose job is partly "produce a side effect" and partly "report a failure" must let the side effect commit and raise the failure afterward — never both from inside the same `try` that a wrapper will roll back. |
| D-27 | 2026-08-24 | `refresh_tokens` table (migration 008) added beyond docs/PROJECT_PROMPT.md §4.2's original schema; refresh tokens are still real JWTs (`jsonwebtoken`, matching the stack's own framing) with this table as a hash-based allowlist alongside them | A bare stateless JWT refresh token with no DB backing | "Rotation with reuse detection" (P1-5's own task description) is impossible with a stateless JWT alone — a signature proves a token wasn't forged, not whether *this specific* token has already been consumed. §4.2 is silent on this because reuse detection wasn't yet a fully worked-out requirement when that schema was written. The JWT signature is still the first, cheaper line of defence against a forged token; the DB row is what actually gates rotation. |
| D-28 | 2026-08-24 | `POST /auth/register` can only self-grant `CUSTOMER` or `ORGANISER`; `ADMIN` is never reachable through the public registration endpoint, regardless of what the request body's `role` field says | Accept and store whatever role the client sends, since `shared/schemas/auth.schema.js` already constrains it to the three valid enum values | The Zod schema only constrains which *strings* are syntactically valid — accepting `{"role":"ADMIN"}` verbatim would let anyone become an admin. `auth.service.js#resolveRegistrationRole` is the actual authorization decision, deliberately kept out of the shared schema so the same schema can later serve an admin-only "create organiser" endpoint that *does* need to accept an arbitrary role from a trusted caller. |
| D-29 | 2026-08-24 | `auth.queries.js#findRefreshTokenByHash` computes `is_expired` in SQL (`expires_at <= now()`) and returns it as `tokenRow.isExpired`; `auth.service.js#refreshTokens` checks that boolean instead of comparing `tokenRow.expiresAt` against `new Date()` | Leave the app-clock comparison as originally written | Found during the `/close-phase 1` audit: CLAUDE.md's "`now()` always comes from Postgres, never the app clock" is stated project-wide, not scoped to the seat-hold TTL mechanism it was first written about. The original `new Date()` comparison would be wrong under clock skew between the app server and the DB server — low-probability on a single dev machine, but exactly the failure mode the invariant exists to rule out categorically, not just where it's currently convenient. Verified live post-fix: a token back-dated directly in Postgres (independent of any JS-side date arithmetic) is correctly detected as expired by `tokenRow.isExpired`. |
| D-30 | 2026-08-24 | Postgres literals (`600`, `900`, `'#6366f1'`) used instead of `COALESCE($n, DEFAULT)` for optional insert columns with a DB-side default | `COALESCE($n, DEFAULT)` to let the column's own `DEFAULT` clause apply when the caller omits a value | `DEFAULT` is only a valid bare token inside a `VALUES (...)` list — Postgres rejects it as an argument to `COALESCE()` (`42601: DEFAULT is not allowed in this context`). Caught live running `shows.queries.js#insertShow` for the first time (P2-5) with `holdTtlSeconds`/`offerTtlSeconds` omitted; `venues.queries.js#insertCategory` had the identical pattern for `colorHex` but was fixed before it was ever exercised live. The literal must be kept in sync with the column's actual default (both commented in place) — a real but small cost next to not being able to omit the field at all. |
| D-31 | 2026-08-24 | `middleware/validate.js` sets `req.query` via `Object.defineProperty(req, 'query', { value, writable: true, ... })` when `source === 'query'`, instead of a plain assignment | Leave the existing `req[source] = result.data` for every source, including `query` | Express 5 defines `req.query` as a getter-only accessor (lazily parses the URL, no setter) — a plain `req.query = ...` throws `TypeError: Cannot set property query of #<IncomingMessage> which has only a getter`. This bug has existed in `validate.js` since P0-4/P1-x, but nothing had ever called `validate(schema, 'query')` until P2-4's `GET /events` browse filters, so it was never exercised. `req.body` and `req.params` remain plain writable properties in Express 5 and don't need the workaround. |
| D-32 | 2026-08-24 | `shared/schemas/event.schema.js#updateEventSchema` is a hand-written object with no field carrying `.default()`, instead of `createEventSchema.partial()` | `createEventSchema.partial()`, extended with `isPublished` | `.partial()` only makes each field optional — it does **not** strip a field's own `.default()`. `createEventSchema.description` is `z.string().optional().default('')`; under `.partial()`, an absent `description` key in the PATCH body still parses into `description: ''`, and `validate.js` hands that fully-defaulted object to the controller as `req.body`. Since `events.queries.js#updateEvent` applies whatever keys are present in the object it's given, a bare `PATCH { isPublished: true }` was silently overwriting the event's real description with an empty string — caught live at P2-4 when a test event's `"A test film"` description turned into `""` after a PATCH that never mentioned it. The fix is a schema where every field is genuinely absent-when-omitted (no `.default()` anywhere), so PATCH semantics (only touch what's sent) actually hold. |
| D-33 | 2026-08-24 | `IllegalSeatTransitionError` maps to HTTP `409`, not `500` | `500 INTERNAL_ERROR` — this represents an application invariant violation (correct code should never construct an illegal transition), which is closer to "the server broke" than to "the request conflicts with resource state" | Neither `docs/PROJECT_PROMPT.md` §4.3 (which names the error and says "unit-test the full matrix," nothing about HTTP status) nor §9's stable-code list pins a status for `ILLEGAL_SEAT_TRANSITION`, so this is a genuine judgment call, not a spec lookup. `409` groups it with `SEATS_UNAVAILABLE`/`OFFER_INVALID` in the "this resource's current state conflicts with the request" family, which is the more honest HTTP-semantics description even though — unlike `SEATS_UNAVAILABLE` — it's expected to never actually fire under normal concurrent load; reaching a client at all means something upstream passed the wrong `fromState` (most likely the seat's raw stored state instead of its effective one). `500` would overstate it as a crash when nothing broke. |
| D-34 | 2026-08-24 | Vitest introduced at P3-1, not deferred to P3-9 | Wait for P3-9 to set up Vitest alongside the DB-backed harness, keep using live-proof scripts for P3-1 the way Phases 0–2 did for everything | `seatState.machine.js#assertTransition()` is pure, synchronous logic with zero I/O — `docs/PROJECT_PROMPT.md` §4.3 explicitly asks for a real "unit-test the full matrix," and a pure function is exactly what Vitest is for, with no dependency on the `ticket_booking_test` database that made every earlier mechanism need a live-proof script instead. Splitting it this way — pure-logic unit tests as soon as there's pure logic to test, the DB-backed `tests/setup/testDb.js` harness and its e2e/concurrency suites still landing at P3-9 as originally planned — means the project's first automated test arrives the moment it's actually possible to write one honestly, rather than either (a) waiting and re-proving P3-1 live like a Phase 0–2 task, which would throw away a real, cheap, permanent regression test for no reason, or (b) building the full DB harness early just to run one file that doesn't need it. |
| D-35 | 2026-08-24 | `acquireSeats()`'s own contract is narrower than `docs/PROJECT_PROMPT.md` §6.6's "loser holds zero seats" claim: for an OVERLAPPING multi-seat request, the raw function can return a genuinely partial, non-empty array for the side that loses only the contested seat — not empty. "Loser holds zero" only becomes true once a caller wraps the call in a transaction and rolls back on any shortfall (P3-3's job); this file's own doc comment and WALKTHROUGH state that explicitly rather than implying `acquireSeats()` itself is all-or-nothing | Design `acquireSeats()` to detect a shortfall internally and roll back to zero itself, so its own return value always matches §6.6's framing directly | Traced mechanically before writing the P3-2 proof (prompted by a user-requested overlapping-set race test): the CTE locks every candidate row for both racing transactions before either outer `UPDATE` evaluates a predicate, so a transaction that loses seat B (contested) can still, independently, win seat A or C (uncontested) in the SAME statement — the RETURNING set reflects exactly that. Confirmed live in the P3-2 proof script (scenario 8, run 8×): the loser's raw `acquireSeats()` result was a genuine 1-of-2 partial array every time, only becoming an effective zero after a wrapping rollback. Making `acquireSeats()` roll back internally would require it to manage its OWN transaction — but its whole contract (`@param client - must already be inside a transaction`) assumes the caller owns that boundary, matching `withTransaction.js`'s single-owner-of-BEGIN/COMMIT design. Keeping the function's real behavior (partial success, not an exception) documented precisely, and pushing the all-or-nothing DECISION to the caller, is more honest than a docstring claiming a guarantee this file doesn't itself enforce. |
| D-36 | 2026-08-24 | `DomainError` gained an optional `details` field (default `null`); `errorHandler.js` now reads `err.details` instead of hardcoding `null` for every domain error | Encode the conflicting seat labels into `SeatsUnavailableError`'s `message` string, parsed or formatted specially by the client | The response envelope's own documented shape (`docs/PROJECT_PROMPT.md` §9) is `{ code, message, details }`, but until now nothing on the `DomainError` path ever populated `details` — only `validate.js`'s separate 422 branch did, for Zod issues. `SeatsUnavailableError` (P3-3) needs to hand the UI a structured list of which seats conflicted, per §6.1 ("so the UI can flash them red") — a string the client has to parse is exactly the kind of fragile inline-error-string pattern CLAUDE.md's conventions rule out. The fix is small, general-purpose (any future `DomainError` can carry `details` the same way), and closes a real gap in the envelope contract rather than working around it for one error class. |
| D-37 | 2026-08-24 | `tests/setup/testEnv.js` (rewrites `DATABASE_URL` to the `_test` variant, checking whether it already ends in `_test` before appending) is a SEPARATE file from `tests/setup/testDb.js` (migrations, truncation, pool close), registered as a Vitest `setupFiles` entry rather than imported at the top of `testDb.js` or of each test file | Fold the DATABASE_URL rewrite into testDb.js itself, relying on plain ESM import ordering within that one file | `setupFiles` entries run to completion, in-process, before Vitest loads the TEST FILE itself — which is what actually matters here, since it's the test file's own imports (e.g. `import app from '../../src/app.js'`) that transitively reach `src/db/pool.js` and trigger its top-level `new Pool({ connectionString: env.DATABASE_URL })`. Plain within-one-file import ordering (`import './testEnv.js'; import {pool} from '...'`) would in fact work for THAT file, but doesn't generalise to "every e2e test file's own top-of-file imports," which is the actual surface that needs protecting. Registering the DATABASE_URL rewrite as `setupFiles` is the mechanism that gives that guarantee; folding it into testDb.js and hoping every test file imports testDb.js before anything else does not. Additionally hardened with a second, DB-level check: `testDb.js#assertTestDatabase()` runs `SELECT current_database()` and refuses `truncateAllTables()`/`migrateTestDb()` outright if the name doesn't end in `_test` — belt-and-braces against a mistake anywhere upstream of the string derivation, since a stray `TRUNCATE` against the real `ticket_booking` would be unrecoverable. |
| D-38 | 2026-08-24 | `tests/setup/fixtures.js#createAdminAndLogin()` inserts an ADMIN user directly via `pool.query(...)`, bypassing `POST /auth/register` entirely, then logs in through the REAL `POST /auth/login` to get its cookie | Add a test-only "seed an admin" backdoor endpoint or environment flag to the app itself | `auth.service.js#resolveRegistrationRole` (D-28) deliberately makes ADMIN unreachable through public registration — that's the correct production behaviour, not a gap to route around inside the app. The test harness bypassing it via direct SQL, once, is a smaller and more honest compromise than adding a "test mode" code path to the actual server that would itself need to be kept from ever being reachable in production. Logging in through the real endpoint afterward means every downstream request in a test uses a cookie that is byte-for-byte indistinguishable from a real admin session's — nothing about `holds`/`venues`/etc. routes is exercised any differently than production traffic would. |
| D-39 | 2026-08-24 | `holds.queries.js#acquireSeats`'s state predicate was temporarily stripped to `WHERE s.id = c.id` (no state check at all) as a live falsification of the new `tests/e2e/concurrency.test.js` suite, then restored and confirmed unchanged via `git status` before the suite was considered done | Trust the new tests without falsifying them, since P3-2/P3-3's underlying mechanism was already proven live in earlier sessions | Matches the project's own established convention (P3-1's `SEAT_TRANSITIONS` deletion test, referenced directly in that task's BUILD_LOG row) that a new automated test isn't trusted until it's been shown to actually fail when the mechanism it claims to prove is removed — otherwise a test asserting the wrong thing, or accidentally not exercising the real code path, would sit in the suite indistinguishable from a real proof. All 3 concurrency tests failed for the specifically expected reason (every racer won the 50-way race; both sides of the overlapping-set race won; the `OFFER_RESERVED` seat was handed out) rather than an unrelated error, confirming the tests exercise the predicate itself and not some other code path. |
| D-40 | 2026-08-24 | `seatState.machine.js#assertTransition()` has no production call site as of Phase 3 — `holds.queries.js`'s `AVAILABLE→HELD` and `HELD→AVAILABLE` transitions happen entirely inside SQL predicates, never through this JS guard | Call `assertTransition()` somewhere in the holds path anyway — e.g. a redundant check in `holds.service.js` before or after the SQL runs | `assertTransition()` guards transitions expressed IN APPLICATION CODE: a caller that already knows a `fromState` and wants a synchronous pre-write check that `(fromState, toState)` is legal before issuing SQL. A transition expressed as a predicate inside ONE atomic `FOR UPDATE` statement (§6.1's acquire, and its symmetric release) cannot call out to a JS function mid-statement without breaking the single-statement atomicity the whole design depends on (CLAUDE.md: "the atomic seat acquire stays one SQL statement... never split it into read-then-write") — wrapping it with `assertTransition()` would require reading the current state first, which is exactly the read-then-write split the design forbids. Not a gap Phase 3 needs to close: naming where the guard WILL actually be exercised instead — Phase 4's `bookings.service.js#confirmBooking()` (`HELD→BOOKED`, a single already-resolved seat, not racing anyone) and Phase 5's `offers.service.js#cascadeOffer()` (`BOOKED→OFFER_RESERVED` and the `OFFER_RESERVED→OFFER_RESERVED` cascade self-loop, both decided in application code before their `UPDATE`, unlike the hold acquire's multi-branch `WHERE`). Found via `grep -r assertTransition server/` returning only its own test file at the Phase 3 close-out audit — flagged explicitly so it doesn't read as dead code left behind by mistake. |
| D-41 | 2026-08-24 | `MAX_SEATS_PER_BOOKING` is enforced in `holds.service.js#createHold()` (a plain `if` before any DB round-trip), not baked into `shared/schemas/hold.schema.js` | Hardcode a seat-count cap into the shared Zod schema so the client can reject an over-large selection before ever calling the API | `MAX_SEATS_PER_BOOKING` is server-only env (`docs/PROJECT_PROMPT.md` §12), read via the validated `env` object — `shared/schemas/hold.schema.js` is imported into the CLIENT bundle too (P7-5) and has no access to server config. Baking a number into the shared schema means either hardcoding a value that can drift from the real env-configured cap (exactly the "magic number in code" CLAUDE.md's Conventions rule out) or plumbing server env into shared/client code, breaking that boundary for one validation rule. Enforcing it as a `ValidationError` in the service layer, which already reads `env`, keeps the cap in exactly one place, correct by construction — at the cost of the client only learning about an oversized selection after a round trip instead of before one, an acceptable trade for a rule that's cheap to violate rarely (P3-3). |
| D-42 | 2026-08-24 | `DELETE /api/v1/holds/:id` (P3-4) is wired with `requireOwnership(loadHoldForOwnership)` — only the hold's own user may release it — even though P3-4's own task text didn't spell out an ownership check | Ship `DELETE /holds/:id` behind `requireAuth` alone (any authenticated user could release ANY hold by id), deferring an ownership check to a later task | `releaseHold()` is deliberately idempotent and side-effect-free to call twice, but that guarantee says nothing about WHO should be allowed to call it once. Without an ownership check, any logged-in customer could guess or enumerate another customer's holdId and free a seat out from under them mid-checkout — a real, exploitable gap in a scored concurrency phase, not a hypothetical. `holds.service.js#loadHoldForOwnership()` reuses the exact same `requireOwnership` factory Phase 2's event/show routes already established (the `{ ownerId }` loader shape), so this cost nothing structurally beyond a one-line loader and route wiring — P3-3's own `docs/FILE_MANIFEST.md` note had already pre-committed to it before P3-4 started. |
| D-43 | 2026-08-24 | P4-1's `payments.service.js` test (`tests/e2e/payments.test.js`) lives in the e2e Vitest config, not `tests/unit/`, even though BUILD_LOG's own P4-1 row originally said "Unit test." It also constructs its own minimal `bookings` row directly via `pool.query` rather than waiting for P4-2's real `confirmBooking()` to exist | Write it as a true unit test (mock the `client`/`pool`) so it could run before any booking-creation code exists | `payments.queries.js`'s functions take a real `client` and write real rows through a real `INSERT`/`UPDATE` — the whole point, per CLAUDE.md, is that mocking `pool`/`client` proves nothing about the actual SQL (the same reasoning `docs/PROJECT_PROMPT.md` §6.6 gives for never mocking the pool in a concurrency test). `vitest.unit.config.js` deliberately has no DB access at all (P3-9) — anything that touches Postgres belongs in the e2e config regardless of how small the module is. Building a real (if minimal) `bookings` row via direct SQL, rather than mocking the FK away, means the test also genuinely exercises `payments.booking_id`'s real foreign key constraint, not an assumption about it. |
| D-44 | 2026-08-24 | `bookings.service.js#confirmBooking()` calls `assertTransition(SEAT_STATES.HELD, SEAT_STATES.BOOKED)` — with `fromState` a hardcoded literal, not derived from a DB read — immediately before `confirmHeldSeats()`'s atomic `UPDATE`, closing the call site D-40 promised | Leave `assertTransition()` uncalled again, since a hardcoded `fromState` can never actually fail and the real correctness guard is `confirmHeldSeats()`'s own `WHERE` clause | The call is trivially true TODAY, and that's fine — D-40 already explained why: it guards a caller's CHOICE of `(fromState, toState)`, a check that only earns its keep once this exact "guarded transaction shape" is reused with a DIFFERENT `fromState`, which §7.3 says will happen at Phase 5's offer-accept (`OFFER_RESERVED → BOOKED`). Skipping the call now and adding it "when it matters" would mean either retrofitting `confirmBooking()` later (touching code that's already shipped and tested) or building the Phase 5 reuse without ever wiring the guard in at all. Wiring it in now, even trivially, means the shared shape both `confirmBooking()` and the future offer-accept function reuse already has the guard in place on day one of that reuse. |
| D-45 | 2026-08-24 | `bookings.service.js#cancelBooking()` also calls `assertTransition(SEAT_STATES.BOOKED, SEAT_STATES.AVAILABLE)` immediately before `releaseBookedSeats()`'s atomic `UPDATE` — a THIRD call site, extending D-40/D-44's reasoning beyond the two originally named (`confirmBooking`, `cascadeOffer`) | Leave `cancelBooking()` without the call, since D-40 never literally named it | Same shape as the other two: a transition decided in application code before a single-row `UPDATE`, not a multi-branch predicate like `holds.queries.js#acquireSeats` needs. D-40's own reasoning was never actually scoped to exactly two functions — it was scoped to a PATTERN ("a transition expressed as a predicate inside one atomic statement... vs. one decided in application code before its `UPDATE`"), and `cancelBooking()`'s `BOOKED → AVAILABLE` step fits that pattern exactly. Calling it here is applying the already-established rule consistently, not inventing a new one — the alternative (only two of the three qualifying call sites actually wired) would be an arbitrary omission with no principled reason behind it. |
| D-46 | 2026-08-24 | `waitlist.queries.js#findQueuePosition()` casts both `ROW_NUMBER() OVER (...)` and `COUNT(*) OVER ()` to `::int` inside the subquery, rather than leaving them as Postgres's default `bigint` | Leave them as `bigint` and let the JSON response carry whatever `pg` returns | Found live, not reasoned about in advance: the first e2e run of `tests/e2e/waitlist.test.js` failed both assertions on `position`/`total`, with `expect(res.body.data.position).toBe(1)` reporting `Received: "1"` — a string, not a number. `pg` returns Postgres `bigint` columns as JS strings by default (a `bigint` can exceed `Number.MAX_SAFE_INTEGER`, which a plain JS number can't represent losslessly), and both window functions are `bigint`-typed even though a queue position or total can never realistically overflow a 32-bit int. The fix is the same `::int` cast the codebase already uses elsewhere for exactly this reason (`shows.queries.js#countShowSeats`, `waitlist.queries.js#countEffectiveAvailableSeats` in this same task) — this function just missed it on the first pass. Casting inside the subquery, not the outer `SELECT`, keeps the outer query trivial. |
| D-47 | 2026-08-24 | `offers.queries.js#transitionBookedSeatsToOfferReserved()` and `insertWaitlistOffer()` each compute `expires_at`/`reserved_until` with their OWN independent `now() + make_interval(secs => $n)` expression, rather than computing one JS timestamp and passing it to both | Compute one `expiresAt`/`reservedUntil` pair in JS (or read one back via `RETURNING` from the first statement) and pass it as a parameter to the second | Relies on a specific, non-obvious Postgres fact worth recording since P5-6's cascade will lean on it again: `now()` (and `CURRENT_TIMESTAMP`) return the START TIME OF THE CURRENT TRANSACTION, not wall-clock-at-statement-execution — they do not advance between statements in the same transaction. Both functions run inside `bookings.service.js#cancelBooking`'s ONE `withTransaction()` call, so two independent `now() + make_interval(secs => $offerTtlSeconds)` expressions are guaranteed to evaluate identically. This keeps CLAUDE.md's "`now()` always comes from Postgres, never the app clock" invariant honest for BOTH values instead of one computed in Postgres and the other threaded through from a JS `Date` — and avoids a timestamp crossing the JS/SQL boundary at all. |
| D-48 | 2026-08-24 | The D-40 call site for `BOOKED -> OFFER_RESERVED` landed in `offers.service.js#createInitialOffer()`, not in a function literally named `cascadeOffer()` | Wait for P5-6 to build `cascadeOffer()` and put the `assertTransition(BOOKED, OFFER_RESERVED)` call there instead | D-40 named `cascadeOffer()` as the anticipated call site before either function existed, but on building P5-3 the natural split turned out to be two functions, not one: `createInitialOffer()` (P5-3, attempt_no 1, called from `cancelBooking()`) and a future `cascadeOffer()` (P5-6, subsequent attempts, called from the `OFFER_EXPIRY` job handler) — the FIRST offer's transition is `BOOKED -> OFFER_RESERVED`, while every SUBSEQUENT cascade attempt is `OFFER_RESERVED -> OFFER_RESERVED` (the self-loop `shared/seatStates.js` documents separately), a different pair with a different natural home. Wiring the guard into `createInitialOffer()` now, rather than waiting for `cascadeOffer()` to exist, follows the same reasoning D-44 already gave for `confirmBooking()`: the shared shape has the guard in place on day one, instead of retrofitting already-shipped code once P5-6 lands. |
| D-49 | 2026-08-24 | P5-3's offer token is a placeholder (`crypto.randomBytes(32).toString('hex')`), not §7.3's real raw-token + sha256-hash shape | Build the real token module now, since it's only a few lines | That few-line shape — generate a raw token, hash it, return the raw token for an email link, enforce single-use on accept — IS P5-4's entire task description ("Single-use HMAC offer token; only the hash stored; raw only in the email link"), not an implementation detail of P5-3's own scope (routing freed seats by category). Building it under P5-3's ticket would mean doing P5-4's work early and calling it done, the same trap CLAUDE.md's plan-per-task rule for Phases 3–5 exists to avoid. Same precedent as P4-2's placeholder `bookings.reference`/`qr_token` generators (deferring the real P4-4/P4-5) — tracked as Phase 5 debt below, replaced wholesale, not extended, when P5-4 lands. Nothing reads this value yet either: `GET`/`POST .../accept` (P5-5) don't exist. |
| D-50 | 2026-08-24 | P5-3 does not enqueue a `job_queue` `OFFER_EXPIRY` row, insert an `outbox_events` `WAITLIST_OFFER` row, or call `pg_notify` when creating the initial offer, despite §7.2's own flow diagram showing all three in the same transaction | Add all three now, since the transaction is already open and each is individually cheap | Each has an already-deferred sibling mechanism this mirrors: the job enqueue is the `OFFER_EXPIRY` analogue of P3-5 (`HOLD_EXPIRY` enqueue, deferred to after Phase 5 by user directive); the outbox insert depends on P4-6/P4-7 (transactional outbox draining + Nodemailer), both also deferred; `pg_notify` is P3-6, deferred alongside P3-5. Correctness does not depend on any of the three: the offer's `expires_at`/`reserved_until` are already enforced by `acquireSeats()`'s existing predicate (proven at P3-2/D-14) with zero workers running, exactly the same Layer-1-alone argument that let P3-3 ship before P3-5 existed. What's missing without them is TIMELINESS (an OFFER_EXPIRY job to trigger the cascade) and VISIBILITY (an email, a live broadcast) — not correctness. Enqueueing a job of a type with no registered handler yet would just sit `PENDING` forever, harmless but also not a real mechanism; better to add it when P5-6/P5-7 actually build something to consume it. Tracked as Phase 5 debt below. **Superseded in part by the 2026-08-24 scope decision to drop P5-6/P5-7/P5-8 outright** (see the note in the Phase 5 table) — the `OFFER_EXPIRY` job and `pg_notify` piece are now simply not coming; only the P4-7 email piece (redefined to send after commit, no outbox) is still planned. |
| D-51 | 2026-08-24 | `waitlist_offers.id` is generated client-side (`crypto.randomUUID()`) and passed explicitly to `offers.queries.js#insertWaitlistOffer()`, instead of left to the column's own `gen_random_uuid()` default | Insert first, read the generated id back via `RETURNING id`, then generate and hash the token, then `UPDATE ... SET token_hash = $1 WHERE id = $2` | §7.3's raw token format is `${offerId}.${randomBytes}` — the offer's own id has to be KNOWN before the raw token (and therefore the hash that gets stored) can be constructed at all, which rules out "insert first, mint the token after" without either a second write (an UPDATE immediately following the INSERT, needlessly two statements for one logical creation) or storing a token that doesn't actually name its own offer's id. Generating the id in JS and threading it through both `generateOfferToken()` and `insertWaitlistOffer()`'s `id` param keeps offer creation a single INSERT, matching every other insert-with-a-derived-value shape already in this codebase (e.g. `bookings.reference` computed before `insertBooking()`, not read back and patched in afterward). |
| D-52 | 2026-08-24 | `generateOfferToken()` uses a plain `sha256(raw)` digest, matching §7.3's literal code sample, rather than a keyed HMAC (`crypto.createHmac`) despite §7.3's own section heading calling this "Single-use HMAC offer token" | Use `crypto.createHmac('sha256', SOME_SECRET)` instead, actually matching the heading's name | An HMAC exists to let two parties who share a secret KEY verify that a MESSAGE came from someone who knows that key, without the message itself being secret. Nothing in this design has that shape: there is no separate message being authenticated and no shared key two different parties hold — the 32 random bytes IN the raw token ARE the entire secret, generated once, known only to whoever received the raw token (via the future claim-link email) and to nobody else until they present it. A plain hash is the correct primitive for "does this presented secret match a stored commitment to it," which is exactly `waitlist_offers.token_hash`'s job; adding an HMAC key would add a second secret (where is it stored? how does it rotate?) for no additional guarantee the random bytes don't already provide. §7.3's own code sample already shows a plain `crypto.createHash('sha256')` call, not `createHmac` — the heading's name for the mechanism doesn't match its own code, and this implementation follows the code. |
| D-53 | 2026-08-24 | **The Phase 7 client is dropped entirely** — no React 18 + Vite + React Router + TanStack Query + Zustand + Tailwind SPA. Replaced with `client/index.html`: one static file, vanilla JS, no framework, no bundler, no build step, served by the Express server itself at `/`. `CLAUDE.md`'s Stack section updated to match (this is a permanent architectural change, not a one-task deviation, so the locked-stack doc itself changes, not just a tracked exception) | Build the originally-specced React/Vite/Tailwind frontend as planned; alternatively, scope down to fewer Phase 7 screens but keep the same framework | User's own reasoning, recorded verbatim: "The brief lists a frontend with a visual seat map and real-time status as a scored requirement, and a hosted URL as a deliverable. Under the remaining time budget a React + Vite + Tailwind SPA could not be completed, and an unattempted requirement scores zero. A single vanilla HTML file served by Express covers the same scored line items — visual grid, per-seat status, seat selection, real-time updates via polling — at roughly a tenth of the build cost, with no bundler, no build step, and no separate deploy target. Framework choice is not scored; the seat map working is." Flagged back to the user before being applied, per `CLAUDE.md`'s own "stop and ask on a conflict with the locked stack" rule (see the session log) — confirmed as a deliberate pivot, not a slip, before this entry or the `CLAUDE.md` edit were made. |
| D-54 | 2026-08-24 | P4-7 sends email directly after a booking/cancellation's transaction commits (from `bookings.controller.js`, fire-and-forget, unawaited) instead of through `outbox_events` + an `OUTBOX_SEND` job (P4-6) — CLAUDE.md's own "emails go through outbox_events" invariant does not hold for this codebase | Build P4-6 first (the outbox table write + job enqueue, both inside the booking's transaction) so P4-7's send has something durable to replay from | User directive, same time-budget reasoning as D-53: build the transactional-outbox machinery (a table write inside the transaction, a job-queue entry, a poller handler to drain it, a reconciler for orphaned rows) or send the email now — under the remaining budget, not both. The correctness cost is real and named, not hidden: a booking or cancellation can now succeed with zero email ever sent (a down mail server, a network blip, the process crashing between commit and the fire-and-forget call resolving) and nothing retries it — unlike the outbox design, where a written-but-undrained row is recoverable by the next poller pass or reconciler sweep. Mitigated, not eliminated, by NOT awaiting the send in the controller: a slow or unreachable SMTP server adds zero latency to the booking response, so the trade is "email might silently not happen," never "booking might fail because of email." `mail/mailer.js`'s own header states this invariant deviation explicitly rather than leaving CLAUDE.md and the code silently disagreeing. |
| D-55 | 2026-08-24 | `client/index.html`'s inline `<script>` block needed helmet's `script-src` CSP directive widened to include `'unsafe-inline'` (`server/src/app.js`) | Move the JS into a separate same-origin file (`<script src="/app.js">`), which the default `script-src 'self'` already covers, matching how the Swagger UI page needed no CSP relaxation at all (D-19) | The user's own instruction for this task was explicit: "ONE vanilla HTML/JS file... Inline CSS and JS." A same-origin external file would satisfy CSP without any relaxation, but splits the "one file" property the instruction asked for. The relaxation is scoped to exactly one directive, not a blanket CSP disable, and every request-derived value the inline script writes to the DOM goes through `textContent`/property assignment, never `innerHTML` with interpolated data (see `client/index.html`'s own header) — so widening `script-src` permits the inline block to RUN at all, it doesn't itself open an injection path, since there's no unescaped-HTML sink for a relaxed CSP to make exploitable. |
| D-56 | 2026-08-24 | `WEB_URL`'s default (`server/src/config/env.js`) changed from `http://localhost:5173` to `http://localhost:3000` (the server's own default `PORT`) | Leave the old `:5173` default now that D-53 already dropped the Vite dev server it pointed at | `WEB_URL` still does real work post-D-53: it's the CORS `origin` (`app.js`) and the base URL `mail/mailer.js#sendWaitlistOfferEmail` builds the waitlist claim link from. Since the client is now served BY this same Express process at `/` (D-53), the correct value is the server's own origin, not a port nothing listens on anymore. `.env.example` and this session's local `.env` (`PORT=4000`) were both updated to match — a stale `WEB_URL` would silently break every browser API call with a CORS error rather than an auth error, exactly the failure mode `.env.example`'s own comment on this var warns about. |

---

## Risk register

| Risk | Impact | Mitigation | Owner task |
|---|---|---|---|
| `pool.query` used inside a transaction, silently escaping it | **Critical** — breaks every correctness guarantee | Every query function takes `client`; lint rule or review check; commented at the top of each `*.queries.js` | P1-3 |
| Client not released on an error path, exhausting the pool | High — total outage under load | `client.release()` in `finally`, always; k6 test watches pool metrics | P1-3, P9-5 |
| Free-tier host sleeps; first grader request times out | High — bad first impression | Uptime pinger + `DEMO.md` warns about a 30s cold start | P10-6 |
| SMTP provider blocks unverified recipients | High — no ticket email | Ethereal in dev (no restrictions, preview URLs); verify a sending domain early for prod; document the sandbox limit | P4-7 |
| `LISTEN` connection drops silently; realtime dies with no error | Medium — UI goes stale, looks broken in the demo | Listener reconnects with backoff; `/health` reports listener state; layers 1 and 3 keep correctness regardless | P3-6 |
| Job poller crashes or is stopped; holds never materialise | Medium | The 30s cron reconciler is independent of the poller and sweeps regardless — this is why layer 3 exists | P3-7 |
| 4 GB RAM: dev server + Vite + tests + browser at once | High — thrashing, confusing failures | Separate `dev:server` / `dev:client` scripts; `docs/TESTING.md` says stop the server before running tests; pool max 10 | P0-3, P9-6 |
| Reviewer reads "no Redis" as a gap rather than a decision | Medium — undersells the work | Dedicated README section arguing it, plus the scaling threshold where Redis *would* be right | P3-8, P10-2 |
| Seat map render lag on 500+ seats | Medium | Memoise `Seat`, batch socket patches, virtualise rows | P7-4 |
| Clock skew between instances affects TTL | Low | Use `now()` from Postgres in every predicate, never the app clock | P3-2 |
| Concurrency tests flaky in CI | Medium — undermines the headline claim | Run 3× in CI; assert exact counts, never "at least" | P3-9 |

---

## Test debt

Mechanisms proven live (real DB, real HTTP, output pasted into `docs/TESTING.md`) but not yet
captured as automated regression tests, because no Vitest/local-test-DB harness exists anywhere
in the project yet — building one just for Phase 1 would be scope creep ahead of P3-9, which is
where that harness is built for real. Tracked here explicitly rather than left implicit, so
"proven once" doesn't quietly get mistaken for "tested" as the codebase grows around it.

| Mechanism | Proven at | Write the automated test at |
|---|---|---|
| `requireRole` — customer on an organiser-only route → 403, organiser → 200 | P1-6 (`docs/TESTING.md`) | P3-9 |
| `requireOwnership` — organiser on someone else's resource → 403, on their own → 200 | P1-6 (`docs/TESTING.md`) | P3-9 |
| Refresh-token rotation — new token differs, same `family_id` | P1-5 (`docs/TESTING.md`) | P3-9 |
| Refresh-token reuse detection — replaying a rotated token revokes the whole family; a second attempt with the "new" token also fails | P1-5 (`docs/TESTING.md`) | P3-9 |
| Bulk seat creation — one call, one `unnest()` INSERT, all-or-nothing on a grid collision | P2-3 (`docs/TESTING.md`) | P3-9 |
| `publishShow()` — materialises exactly one `show_seats` row per active venue seat; republishing → `409`, no duplicate rows | P2-6 (`docs/TESTING.md`) | P3-9 |
| **Seatmap effective-state (lazy expiry, Layer 1)** — a manually stale `HELD` row reads as `AVAILABLE` with zero workers running | P2-7 (`docs/TESTING.md`) | P3-9, alongside `holdExpiry.test.js` — this is the same mechanism §5.2's Layer 1, just proven before Layers 2/3 (the job and the cron) exist to compete with it |
| Category/event/show ownership and role gates (organiser-vs-organiser, admin-vs-organiser) across the new Phase 2 routes | P2-1..P2-6 (`docs/TESTING.md`) | P3-9 |

**P3-9 scope note:** the Vitest/Supertest e2e harness this table points at now exists
(`server/tests/setup/testEnv.js`, `testDb.js`, `fixtures.js`), but P3-9 itself only used it to
build the concurrency proof suite (`docs/PROJECT_PROMPT.md` §6.6) — it did not also write
regression tests for every row above. Every mechanism in this table is still only proven live in
`docs/TESTING.md`, not yet an automated test. The harness existing removes the *reason* this was
deferred (no test infra); actually closing each row is still open work, not silently resolved by
P3-9 landing.

---

## Phase 2 debt

Phase 2 is CRUD scaffolding for Phase 3 to sit on, not a scored mechanism (CLAUDE.md's Priority
section: "if time runs short, cut screens — never cut Phase 3, 4 or 5"). Corners deliberately cut
for speed, tracked here so they don't get mistaken for oversights later:

- **Generic `CONFLICT` error code** for every unique-constraint violation (duplicate category
  name, duplicate seat grid position, double-publish) instead of one code per resource. See
  `shared/errors.js`'s own comment on `CONFLICT`.
- **No client-side duplicate/collision pre-check** before `venues.queries.js#insertSeatsBulk` —
  the DB's `UNIQUE` constraints are the only thing catching a bad request; a large request with
  one bad row rolls back entirely rather than reporting which row was the problem.
- **No cross-venue validation** on bulk seat creation (`categoryId` must exist, but nothing checks
  it belongs to *this* venue) or show pricing (`prices[].categoryId` must exist, but nothing
  checks it belongs to the show's venue). A category from Venue A could be attached to a seat or a
  show price for Venue B; only a raw FK violation (still caught, still a clean error) stops a
  fully bogus id.
- **`publishShow()`'s "already published?" check is a plain check-then-act**, not a single atomic
  statement — deliberately, since publishing is an admin-only, one-time-per-show action, not a
  high-contention path the way seat acquisition is. `UNIQUE (show_id, seat_id)` is the real
  backstop if two publish clicks ever do race (see the WALKTHROUGH comment on
  `shows.service.js#publishShow`).
- **Malformed `:id` path params** (not a valid uuid) surface as a raw Postgres error → generic
  `500 INTERNAL_ERROR`, not a clean `400`/`404` — no route validates path params as uuids.
- **`GET /events/:id` does not gate by `is_published`** — an organiser's unpublished draft is
  fetchable by anyone who has (or guesses) its id. Intentional for this phase (an organiser needs
  to fetch their own draft to attach shows to it) and documented in `events.service.js#getEvent`,
  but real product behaviour a later phase should revisit with a proper "is this the owner"
  branch instead of an open read.
- **No DELETE routes** for venues, categories, seats, events, or shows — not in
  `docs/PROJECT_PROMPT.md` §9's endpoint list, so none were built.
- **No validation that a show's `prices[]` covers every category the venue actually has** — a show
  can be created pricing only some of its venue's categories; the seatmap already reports
  `priceCents: null` correctly for an unpriced category's seats (a `LEFT JOIN`, not an `INNER
  JOIN`), but nothing stops that state from being created in the first place.
- **Venue read routes gated to `ADMIN` + `ORGANISER`**, not just `ADMIN` — a judgment call (an
  organiser needs to browse venues/categories to build a show) documented inline in
  `venues.routes.js`, not literally specified either way in §9.

---

## Phase 3 debt

Unlike Phase 2, Phase 3 is the scored mechanism — corners here are genuine scope decisions with a
plan to close them, not "speed over polish." Tracked explicitly so a deliberate deferral doesn't
read as an oversight later.

- **`holds.service.js#createHold()` does not check `shows.status`** — a hold can currently be
  created against a `CANCELLED` or `COMPLETED` show; nothing in P3-3 rejects it. Deferred on
  purpose (user directive, 2026-08-24): out of scope for the concurrency mechanism P3-3 is about.
  Revisit when a task naturally needs show-lifecycle validation — likely Phase 4 (booking) or
  wherever `SHOW_NOT_SELLABLE` (already a named code in `docs/PROJECT_PROMPT.md` §9) gets its
  first thrower. **Still open as of P4-2** — `confirmBooking()` doesn't check it either, for the
  same reason.

## Phase 4 debt

Like Phase 3, this is scored territory (`confirmBooking()`/`cancelBooking()` are both starred
mechanisms) — corners cut here are genuine, tracked scope decisions, not polish skipped for speed.

- **`bookings.reference` and `bookings.qr_token` are placeholder-generated**, not the real P4-4
  (Crockford base32, no ambiguous characters, 100k-collision-tested) or P4-5 (signed JWT → PNG
  buffer) implementations. `bookings.service.js#generatePlaceholderReference()` /
  `generatePlaceholderQrToken()` are neither cryptographically meaningful nor collision-tested —
  deferred on purpose (user directive, 2026-08-24) to keep P4-2 scoped to the §6.5 mechanism
  itself. Both are replaced wholesale, not extended, when P4-4/P4-5 land for real; every call site
  (`confirmBooking()`, and whatever P4-8/P4-9 add) keeps working unchanged since only the
  generators' internals change, not their signatures.
- **`Idempotency-Key` header handling doesn't exist** — a double-submitted confirm is still safe
  (proven live: the 20-parallel-confirms test), but via the `expires_at`/`state` guard racing
  correctly, not via `bookings.idempotency_key`. A retried request after the FIRST one already
  won gets a `410 HOLD_EXPIRED`, not the original booking replayed back — technically correct
  (nothing double-books) but not the UX §9's `Idempotency-Key` header implies. Lands with P4-3.
- **`confirmBooking()` doesn't check `shows.status`** — same gap `holds.service.js#createHold()`
  already has (Phase 3 debt above), inherited rather than newly introduced.
- **No `customer{}` field in `confirmBookingSchema`**, despite §9 listing
  `POST /bookings/confirm { holdId, customer{} }` — nothing in the `bookings` table needs customer
  contact info beyond `user_id` (the authenticated caller). A deliberate simplification, not an
  oversight; revisit only if a real requirement for it surfaces (e.g. a guest-checkout flow this
  project doesn't currently plan to build).
- **P4-7's email has no durability** (Decisions Ledger D-54) — a booking or cancellation can
  succeed with zero email ever sent (SMTP down, network blip, process crash between commit and
  the fire-and-forget send resolving), and nothing retries it. `outbox_events`/`OUTBOX_SEND`
  (P4-6, dropped) would have made a written-but-undrained row recoverable; this design has no
  equivalent. Mitigated only in the other direction: the send is never awaited, so a slow/down
  mail server cannot make a booking request itself slow or fail.
- **QR content is still P4-2's placeholder token** (`QR-${uuid}`), not §8's real signed JWT
  (P4-4/P4-5, both still unbuilt) — P4-7 renders whatever `booking.qr_token` holds into a PNG;
  what that token cryptographically IS remains open Phase 4 debt from before this task.

---

## Phase 5 debt

Like Phase 3/4, this is scored territory (waitlist auto-assignment and time-limited offers are
named explicitly in `docs/PROJECT_PROMPT.md` §0 as one of the four hard problems) — corners cut
here are genuine, tracked scope decisions, not polish skipped for speed.

- ~~The offer token is a placeholder~~ — **resolved at P5-4**: `offers.service.js#generateOfferToken()`
  is now the real §7.3 raw-token + sha256-hash shape (D-51/D-52 cover the two non-obvious calls:
  generating the offer's own id client-side so it can be embedded in the raw token, and using a
  plain hash rather than a keyed HMAC despite §7.3's section heading). ~~Nothing reads `rawToken`
  yet~~ — **resolved at P4-7 (email) + P5-5 (claim/accept)**: the waitlist-offer email carries a
  real claim link, and `GET`/`POST /waitlist/offers/:token` now genuinely resolve it end to end,
  proven live including a real concurrent-accept race (see P5-5's own row).
- **Cascade-on-lapse (§7.4) is dropped, not deferred** — see the Phase 5 table's 2026-08-24 scope
  decision. `claimNextWaitingEntry()` (P5-3) is the one piece already built and reused; the
  `OFFER_EXPIRY` job handler, its cron/poller wiring, and the re-offer/attempt_no+1 logic that
  would call it are not coming. An offer that lapses without being accepted stays `OFFER_RESERVED`
  until `reserved_until` (Layer 1 still applies), but nobody actively offers it to the next person
  in line.
- **No `job_queue` `OFFER_EXPIRY` row, `outbox_events` `WAITLIST_OFFER` row, or `pg_notify` call**
  is written when P5-3 creates the initial offer, despite §7.2's own diagram showing all three in
  the same transaction (Decisions Ledger D-50). Each mirrors an already-deferred sibling: the job
  enqueue is `HOLD_EXPIRY`'s counterpart (P3-5, deferred to after Phase 5); the outbox row depends
  on P4-6/P4-7 (also deferred); `pg_notify` is P3-6 (also deferred). Correctness doesn't depend on
  any of the three — the offer's `expires_at`/`reserved_until` are already enforced by
  `acquireSeats()`'s existing predicate with zero workers running (the same Layer-1-alone argument
  P3-3 already relied on before P3-5 existed). What's missing is TIMELINESS (an active cascade
  trigger) and VISIBILITY (email, live broadcast), not correctness. As of the P5-6/P5-7/P5-8 drop
  above, an `OFFER_EXPIRY` job would never be enqueued in the first place — there's no longer a
  planned handler for it to wait for. `pg_notify` stays possible independent of this (P3-6, still
  formally deferred rather than dropped) if realtime work resumes later.
- **`claimNextWaitingEntry()` doesn't check `shows.status`** — same gap `holds.service.js#createHold()`
  and `confirmBooking()` already have (Phase 3/4 debt above), inherited rather than newly
  introduced. A cancellation on a `CANCELLED`/`COMPLETED` show can still create a live offer.
- **Multi-seat waitlist entries (`quantity > 1`) aren't matched against how many seats a
  cancellation actually frees.** `createInitialOffer()` offers EVERY seat a category's cancelled
  booking freed to the queue head, regardless of that entry's own `quantity` — a `quantity: 1`
  entry could be offered 3 seats if that's how many the cancelled booking held in that category.
  §7's own spec doesn't resolve this either; revisit if a real requirement for partial-match
  offers surfaces.

---

## Changelog

Append on every merge to `main`. Keep-a-Changelog format, Conventional Commits.

```
## [Unreleased]
### Added
### Changed
### Fixed
```

**Example entry, for format:**

```
## [0.3.0] — 2026-XX-XX
### Added
- feat(holds): atomic seat acquire via FOR UPDATE CTE with deterministic ordering [P3-2]
- feat(holds): three-layer TTL — SQL predicate, in-transaction job row, cron reconciler [P3-5..P3-7]
- test(concurrency): 50-parallel-hold suite on local test DB; asserts exactly one winner [P3-9]
### Changed
- refactor(seatmap): all state transitions routed through seatState.machine.js [P3-1]
### Fixed
- fix(holds): releaseHold() is now idempotent; concurrent releases no longer throw [P3-4]
```

---

## Session log

| Date | Tasks worked | Commits pushed | Outcome | Next up |
|---|---|---|---|---|
| 2026-08-23 | P0-1 | 1 (see commit hash reported in chat) | npm workspaces (`server`/`client`/`shared`) live, ESM throughout, `npm install` clean. Also fixed `.gitignore` accidentally excluding spec docs, and relocated `FILE_MANIFEST.md` into `docs/` to match spec references | P0-2 |
| 2026-08-23 | D-14 (spec fix, no BUILD_LOG task) | 1 (see commit hash reported in chat) | Resolved the §6.1/§7.2 offer-window contradiction with `reserved_until`; appended the READ COMMITTED verification to D-2 | P0-2 |
| 2026-08-23 | P0-2 | 1 (see commit hash reported in chat) | ESLint 10 flat config (`eslint.config.js`, not the legacy `.eslintrc.json` the spec named — self-contradiction caught, see D-15) + `eslint-plugin-jsdoc` scoped to `server/src`/`shared` + Prettier + `jsconfig.json`. `npm run lint` clean; jsdoc rule verified live against a throwaway undocumented export | P0-3 |
| 2026-08-23 | P0-3 | 1 (see commit hash reported in chat) | `.gitattributes` + `docs/DEPLOYMENT.md` (local env repro) added. `postgresql.conf` verified/edited directly on disk (`shared_buffers` already correct, `max_connections` 100→50); restart to make it live blocked on the shell not running elevated — flagged for you to run manually, see D-16 | P0-4 |
| 2026-08-23 | P0-4 | 1 (see commit hash reported in chat) | Express skeleton: helmet/CORS+credentials/cookie-parser/JSON body/pino-http, health router, 404 fallback, error handler last. `shared/errors.js` and `errorHandler.js` created minimally (2 generic codes) rather than left as stubs or inlined strings, see D-17. Verified live against a running process (`/health` → 200, `/nope` → 404, a throwaway thrown-error route → 500 with no stack leak, then removed) | P0-5 |
| 2026-08-23 | P0-5 | 1 (see commit hash reported in chat) | Zod (v4.4.3) env validation + `.env.example`, closing the loop on P0-4's provisional `process.env` reads (`app.js`/`index.js` now read the validated `env` object). Boot-fails-loudly proven live 3 ways: missing required vars, a valid boot with defaults applied, and the D-10 QR/JWT-secret-collision check actually firing. See D-18 for the required-vs-defaulted split and the native `process.loadEnvFile()` call instead of adding `dotenv`. Also: accidentally deleted a real in-progress user file (`.claude/skills/batch/SKILL.md`), caught mid-save looking empty, mistook for a stray artifact — restored it in a follow-up commit, flagged clearly | P0-6 |
| 2026-08-23 | P0-6 | 1 (see commit hash reported in chat) | Swagger/OpenAPI at `/api/docs` + `/api/docs.json`. Live-testing (not trusting config on faith) caught a real Windows-only bug — `path.join()`'s backslashes silently zeroed out the route-scanning glob — and disproved my own initial assumption that a CSP override was needed for the docs page to render. See D-19 for both | P0-7 |
| 2026-08-23 | P0-7 | 1 (see commit hash reported in chat) | CI workflow: install/lint/test against a GitHub-hosted Postgres service container, with the DB URL and secrets as plain inline fake values rather than repo Secrets so a fork/clone goes green without extra setup (D-20). **Phase 0 complete, 7/7.** YAML validated with `js-yaml` and the exact CI commands run locally, but the actual GitHub Actions run itself is unverified — no `gh` CLI here | Run `/close-phase 0` before merging to `main` |
| 2026-08-23 | `/close-phase 0` audit | 2 (see commit hashes reported in chat: fixes, then the merge) | First pass: **NOT CLEAR** — `npm run dev:server` (the literal exit criterion) didn't exist as a script, CI status was still unconfirmed, and the working tree had an uncommitted deletion of `.claude/skills/batch/SKILL.md`. User confirmed CI green (run #1, commit `9d11e0f`, 48s), asked for the file restored, and asked for `dev:server` added — all three done, verified live (`npm run dev:server` from repo root actually boots and serves `/health`). See D-21. Second pass re-audited those three points only: **CLEAR**. `phase/0-foundation` fast-forward merged into `main` (no divergence — `main` was 0 commits ahead), tagged `v0.1.0-phase0`, both pushed. `main` didn't exist on the remote before this push | P1-1 |
| 2026-08-23 → 2026-08-24 | `/batch P1-1 to P1-4` | 1 (see commit hash reported in chat) | Branched `phase/1-db-auth` off `main`. Built all 7 migrations (schema from PROJECT_PROMPT.md §4.2, with the `show_seats.booking_id` FK deliberately deferred from 003 to 004 to resolve a real forward-reference the file-grouping created), `pool.js`, `withTransaction.js`, `enqueue.js`, `poller.js`, `backoff.js`, and `migrate.js`. Every verification criterion run live against the real `ticket_booking` database, not summarised or assumed — full output pasted into the new `docs/TESTING.md`: migrations up/down/reset all clean, `withTransaction` rollback + `idleCount` (not just `totalCount`) returning to 1 after each of 6 failed transactions, and 21 total poller-race trials with exactly one claim every time. Two real bugs found and fixed mid-batch, not just config choices — D-22 (node-pg-migrate's `--envPath` silently no-ops without `dotenv`) and D-23 (`z.coerce.number()` turns `""` into `0`, not `NaN`) — plus a third, D-24, fixing a P0-2 ESLint config comment that had been wrong since it was written but hadn't mattered until this batch's first unexported `function` declarations | P1-5 |
| 2026-08-24 | `/batch P1-5 to P1-7` | 1 (see commit hash reported in chat) | Full auth module (register/login/refresh/logout/me, argon2, JWT access+refresh, httpOnly cookies), `requireAuth`/`requireRole`/`requireOwnership` middleware, and the seed skeleton. Added `refresh_tokens` (migration 008, D-27) and 5 auth error codes beyond what §4.2/§9 name explicitly. Two real, security-relevant bugs found and fixed by actually running the reuse-detection proof against the real DB instead of trusting the code by inspection: a `jti`-less refresh JWT could hash-collide within the same second (D-25), and — the more serious one — the reuse-detection branch was throwing from inside the same transaction that revoked the token family, so `withTransaction`'s unconditional rollback silently undid the revocation every time (D-26). RBAC/ownership proven over real HTTP via temporary routes since no `events` table exists yet to test against for real (D-28 covers the registration role-escalation guard found along the way). Full proof output in `docs/TESTING.md`. **Phase 1 complete, 7/7.** | `/close-phase 1` |
| 2026-08-24 | `/close-phase 1` audit | 2 (see commit hashes reported in chat: fix, then the merge) | First pass found one real invariant violation (`auth.service.js` checking refresh-token expiry via `new Date()` instead of Postgres's `now()`, contradicting CLAUDE.md's project-wide app-clock rule) and one accepted gap (RBAC/ownership/rotation proven live but not yet automated tests, since no Vitest harness exists — that's P3-9's job). User approved fixing the first and explicitly deferring the second: fixed `auth.service.js`/`auth.queries.js` to compute expiry in SQL (D-29), re-verified live (a token back-dated directly in Postgres, independent of any JS date arithmetic, correctly caught as expired), and added the "Test debt" section tracking what P3-9 needs to write. Second pass re-audited that one point: **CLEAR**. `phase/1-db-auth` merged into `main`, tagged `v0.2.0-phase1`, both pushed | P2-1 |
| 2026-08-24 | `/batch P2-1 to P2-7` | 1 (see commit hash reported in chat) | Branched `phase/2-venues-shows` off `main`. Built venues (CRUD + categories + bulk seat creation), events (CRUD + ownership-guarded PATCH + public browse), shows (creation with per-category pricing, ⭐ `publishShow()`), and seatmap (⭐ effective-state read — Layer 1 of the TTL design, live before Phase 3 exists to build Layers 2/3). Explicitly speed-over-polish per this phase's instructions — see "Phase 2 debt" above. Three real bugs found and fixed by actually running every endpoint against the real DB/HTTP instead of trusting the code by inspection, not just config choices: D-30 (`COALESCE($n, DEFAULT)` isn't valid Postgres — caught creating a show with default TTLs), D-31 (Express 5's `req.query` is a getter with no setter — `validate.js` had carried this bug since P0-4/P1-x, unexercised until P2-4's first query-validated route), and D-32 (`.partial()` doesn't strip Zod `.default()` — a bare `PATCH {isPublished:true}` was silently wiping an event's `description`, caught by checking the DB's actual state after the call, not just the response). **P2-7's lazy-expiry proof run exactly as requested**: manually `UPDATE`'d a `show_seats` row to `HELD` with `expires_at` an hour in the past, confirmed via `RETURNING` that the raw row really was stale, then called `GET /shows/:id/seatmap` and got `AVAILABLE` back for that seat — with no Phase-3 worker or scheduler running to have done it any other way. Full transcripts in `docs/TESTING.md`. No `npm test` yet (script doesn't exist until P3-9's Vitest harness, consistent with Phase 0/1) — `npm run lint` clean. **Phase 2 complete, 7/7.** | `/close-phase 2` |
| 2026-08-24 | `/close-phase 2` audit | 0 (audit only, no fixes needed) | All 7 exit-criteria/row/manifest/invariant/Ledger checks passed on the first pass: **CLEAR**, with one non-blocking nit (the generic `CONFLICT` code is documented in code comments and "Phase 2 debt" rather than as its own numbered Decisions Ledger entry). No automated test suite yet — expected and tracked, consistent with Phases 0/1; not a Phase-2-specific regression. Verdict reported; merge was offered but not executed in that turn (user hadn't yet responded) | (merge pending) |
| 2026-08-24 | Phase 2 merge (caught late) | 2 (see commit hashes reported in chat: merge, then this doc update) | User opened the next session with "Phases 0-2 complete and merged" while asking to branch Phase 3 — checking `git log`/`git branch -a` first (per standing practice: verify before trusting a stated premise) showed `main` was still at the Phase 1 close-out commit and `phase/2-venues-shows` had never actually been merged, despite the prior turn's CLEAR verdict. Completed the merge the prior turn's verdict already authorized rather than silently branching Phase 3 off a `main` missing all of Phase 2: fast-forward merged `phase/2-venues-shows` into `main`, tagged `v0.2.5-phase2`, both pushed | P3-1 |
| 2026-08-24 | `/next-task` → P3-1 | 1 (see commit hash reported in chat) | Branched `phase/3-holds-concurrency` off the now-correct `main`. Read §4.3, §5, §6, §7 of `docs/PROJECT_PROMPT.md` in full (not from memory, per user instruction) and confirmed D-14 before writing anything. Built `shared/seatStates.js` (`SEAT_STATES` + `SEAT_TRANSITIONS`, both deep-frozen — caught mid-build that `Object.freeze()` alone is shallow and wouldn't stop `SEAT_TRANSITIONS.HELD.push(...)`, fixed before it shipped rather than after a test found it), `seatState.machine.js#assertTransition()`, the `ILLEGAL_SEAT_TRANSITION` code/error class (D-33), and introduced Vitest for the project's first automated test (D-34). Full 5×5 matrix + 2 unrecognised-state + 4 immutability cases, 31/31 green. Falsified live per project convention: deleted the `OFFER_RESERVED → OFFER_RESERVED` cascade entry, confirmed exactly 1 test failed (that exact pair), restored, re-ran green. User's two additions to the plan (assert `SEAT_TRANSITIONS` frozen including a real write attempt; a WHY comment naming the self-loop as the cascade case) both incorporated before implementation, not bolted on after. **First Phase 3 task complete, 1/9.** | P3-2 |
| 2026-08-24 | `/next-task` → P3-2 | 1 (see commit hash reported in chat) | Re-read §4.3/§5/§6/§7 fresh (not reused from the prior task's reading) and `FILE_MANIFEST.md`'s holds rows before planning, per instruction. Built `holds.queries.js#acquireSeats()` — the literal §6.1 CTE, unmodified from spec since D-14's fix is already baked into the doc text — with the mandatory numbered WALKTHROUGH comment (the four-hard-mechanism requirement, CLAUDE.md). User's approved-plan addition (an overlapping-set `{A1,A2}` vs `{A2,A3}` race, asserting the loser ends at zero seats and no row is left half-held) surfaced a real, non-obvious fact about this function's actual contract before any code shipped: traced mechanically that the CTE locks every candidate row for both racers before either predicate evaluates, so the side that loses only the CONTESTED seat can still independently win an uncontested one in the same statement — the raw return is a genuine partial array, not empty. "Loser holds zero" only becomes true once a caller (P3-3, not yet built) wraps this in a transaction and rolls back on any shortfall. Documented as D-35 and written directly into the WALKTHROUGH comment so a reader doesn't have to rediscover it. Proven live via a throwaway Node script (not committed, deleted before commit) against the real `ticket_booking` DB: 6 single-scenario predicate cases (including both halves of D-14), a 2-seat partial-success case, a single-seat 2-way race, and the overlapping-set race wrapped in throwaway "simulate P3-3" transaction scaffolding — run 8× with zero deadlocks, exactly one full winner each time, and the DB always left in one of exactly two valid final states, never inconsistent. Full transcripts in `docs/TESTING.md`. `npm run lint` clean; existing 31/31 Vitest suite unaffected (P3-2 needed no new automated test — DB-backed harness is still P3-9's). **2/9.** | P3-3 |
| 2026-08-24 | `/next-task` → P3-3 | 1 (see commit hash reported in chat) | Re-read §4.3/§5/§6/§7 fresh and the holds/`hold.schema.js` `FILE_MANIFEST.md` rows before planning. Built `holds.service.js#createHold()`, `holds.controller.js`, `holds.routes.js` (`POST /` only — `GET`/`DELETE` deferred to whichever task needs them), and `shared/schemas/hold.schema.js`. Both scope calls from the plan approved as proposed: `MAX_SEATS_PER_BOOKING` enforced in the service (schema can't read server env); `shows.status` check explicitly deferred, logged under the new "Phase 3 debt" section per instruction, not silently dropped. Real infra gap found and fixed: `DomainError` had no way to carry structured data to the client — `errorHandler.js` hardcoded `details: null` for every domain error — so `SeatsUnavailableError`'s conflicting-seats payload had nowhere to go; added `DomainError.details` and fixed `errorHandler.js` to read it (D-36). The shortfall-rollback check carries the exact WHY comment requested — naming D-35, stating plainly that `acquireSeats()` is not itself all-or-nothing and that removing the check because "the query already handles it" reintroduces a silent partial hold. **User's proof addition incorporated**: Scenario B's live transcript explicitly captures `seatHoldsCountBefore === seatHoldsCountAfter === 0`, proving the parent `seat_holds` row from a rolled-back attempt doesn't survive as an orphaned ACTIVE hold. Proven fully E2E over real HTTP (register → cookie → `POST /holds` against a running server): available seat → 201; partial conflict → 409 with correct labels + full rollback confirmed both in the response and by direct DB query; `MAX_SEATS_PER_BOOKING` → 422; the overlapping-set race over real concurrent HTTP, run 6×, one 201/one 409 every time, `seat_holds` row count always exactly 1. Full transcripts in `docs/TESTING.md`. Per user directive, P3-5 through P3-8 marked deferred-to-after-Phase-5 in the Phase 3 table (rationale recorded there) — going straight to P3-9 next. `npm run lint` clean; 31/31 Vitest unaffected. **3/9.** | P3-9 |
| 2026-08-24 | `/next-task` → P3-4 | 1 (see commit hash reported in chat) | Re-read §4.3/§5/§6/§7 fresh before planning. Flagged up front that P3-4's original "Verified by" (Called 3× → one broadcast) assumes P3-6, which is deferred — adapted to DB-state-only verification, noted in the Phase 3 table so the broadcast half stays visibly owed to P3-6, not silently dropped. Built `holds.service.js#releaseHold()` with the numbered WALKTHROUGH comment CLAUDE.md mandates for this exact function, `holds.queries.js#releaseHoldSeats/markSeatHoldReleased/findSeatHoldById`, and — a scope call flagged and approved, not decided silently — `DELETE /api/v1/holds/:id` wired with `requireOwnership` (only the hold's own user may release it; not in P3-4's literal text, but a real gap if skipped in a scored phase, and already pre-committed to in P3-3's `FILE_MANIFEST.md` note). **User's proof addition incorporated**: released a STALE holdId whose seat had, in between, been lazily reclaimed under a brand-new hold via the REAL `createHold()` path — confirmed not just that the new holder's seat was untouched, but that the new holder's own `seat_holds` row stayed `ACTIVE` throughout, while the stale hold's own row correctly transitioned to `RELEASED`. This is the exact case that would silently break if `markSeatHoldReleased`'s predicate were ever refactored to join through a seat instead of keying on the hold's own `id`. Live E2E over real HTTP, 4 scenarios, all correct on first run — no new bug found this task, everything behaved as designed. Full transcripts in `docs/TESTING.md`. `npm run lint` clean; 31/31 Vitest unaffected. **4/9.** | P3-9 |
| 2026-08-24 | P3-9 (planned in a prior session; this session executed the approved plan) | pending (see commit hash reported in chat) | User approved the previously-drafted plan with two answers: (1) log the 3 §6.6 scenarios needing modules that don't exist yet (bookings, waitlist/offers) in `docs/BUILD_LOG.md` now, and annotate `docs/PROJECT_PROMPT.md` §6.6's own table with where each moves — done, not deferred silently; (2) build the 4 buildable scenarios for real. Built the permanent Vitest/Supertest harness `docs/BUILD_LOG.md`'s P3-9 row describes in full: `vitest.unit.config.js`/`vitest.e2e.config.js` (split because the two suites have opposite concurrency needs — see D-37), `tests/setup/testEnv.js` (DATABASE_URL → `_test` derivation, checking for CI's Postgres container already being named `ticket_booking_test` before appending), `tests/setup/testDb.js` (migration + truncation + pool close, with a live `current_database()` guard beyond the string check), `tests/setup/fixtures.js` (`registerAndLogin`/`createAdminAndLogin`/`buildBookableShow` — D-38 covers the ADMIN bypass). `tests/e2e/concurrency.test.js` (50-parallel-hold race, the overlapping `{A1,A2}`/`{A2,A3}` race, the D-14 `OFFER_RESERVED`-vs-public-hold check) and `tests/e2e/holdExpiry.test.js` (lazy-expiry re-proof, zero schedulers running) all pass locally against the real `ticket_booking_test` database. **Falsified live before being trusted** (D-39, same convention as P3-1): stripped `acquireSeats()`'s state predicate to `WHERE s.id = c.id`, all 3 concurrency tests failed for the expected reason, predicate restored and confirmed byte-for-byte unchanged via `git status`. `npm run test:concurrency` run 3× locally, 3/3 clean — matching the loop now wired into `.github/workflows/ci.yml` (untested by this session; no `gh` CLI access, same caveat as every other CI change so far). `npm run lint` clean throughout. Full transcripts in `docs/TESTING.md`. **P3-9 complete, 5/9.** | `/close-phase 3` once P3-5..P3-8 land post-Phase-5, per the existing deferral note |
| 2026-08-24 | `/close-phase 3` audit + fixes | 1 (see commit hash reported in chat) | First pass found the P3-9 suite proved what it set out to prove but had two real gaps, both falsified live to confirm they were real before being fixed, not assumed. **(1)** `holdExpiry.test.js` only covered the READ side of "seats free themselves" — removing `acquireSeats()`'s `HELD`-and-expired OR-branch left the whole e2e suite green. Added a write-side test (user B's real `POST /holds` reclaims a seat whose `expires_at` lapsed under user A's hold); falsified the same way, confirmed it now fails for the right reason, restored, confirmed `git status` clean. **(2)** P3-4's `releaseHold()` had no permanent test at all, only session-script transcripts. Added `tests/e2e/holds.test.js` (idempotent double/triple release, non-owner 403, stale-holdId safety); falsified the stale-holdId case specifically by changing `markSeatHoldReleased()`'s predicate to match "the show's active hold" instead of "this hold's own id" — caught a live, unrelated hold's `ACTIVE` row silently flipping to `RELEASED`, exactly the corruption mode CLAUDE.md's commenting standard on that function warns about; restored, confirmed clean. Also added the three Decisions Ledger entries the audit's point 7 found missing: D-40 (`assertTransition()` has no production call site — a design boundary, not an oversight, naming Phase 4/5 as where it will actually be exercised), D-41 (`MAX_SEATS_PER_BOOKING` enforced in the service layer, not the shared schema), D-42 (`requireOwnership` wired on `DELETE /holds/:id`). Second pass re-audited only these points: **CLEAR**. `npm run test:unit` 31/31, `npm run test:e2e` 8/8 (3 files), `npm run lint` clean. Full transcripts in `docs/TESTING.md`. `phase/3-holds-concurrency` fast-forward merged to `main`, tagged `v0.3.0-phase3`, both pushed. **Phase 3 closed (deliberately partial — P3-5..P3-8 still deferred to post-Phase-5).** | 2 (fixes, then this doc update) |
| 2026-08-24 | Phase 4/5 reorder (user directive) | 1 (see commit hash reported in chat) | User directed P4-2 (`confirmBooking`) and P4-8 (`cancelBooking`) to move ahead of the rest of Phase 4, with ALL of Phase 5 built immediately after, since §7.2/§7.3's cascade and offer-acceptance flow are built directly on `cancelBooking()`/`confirmBooking()`. While updating `docs/BUILD_LOG.md` to record this, found a real (not just numerical) dependency: `bookings.reference`/`qr_token` are `NOT NULL`, so P4-4 (reference generator) and P4-5 (QR service) are hard prerequisites of P4-2; `cancelBooking()`'s own "refund" requirement pulls P4-1 (mock payments) forward too. Documented the real build order (P4-4 → P4-5 → P4-1 → P4-2 → P4-8 → Phase 5 → rest of Phase 4) directly in the Phase 4 table rather than silently reordering rows. Branched `phase/4-booking-qr` off `main`. No code changes. | P4-1 (lowest-numbered unblocked task per `next-task`'s own rule) |
| 2026-08-24 | `/next-task` → P4-1 | 1 (see commit hash reported in chat) | Plan mode used per CLAUDE.md's Phase 3/4/5 rule. Built `payments.queries.js`/`payments.service.js` — `authorizeAndCapture()` (two writes, `AUTHORIZED` then `CAPTURED`, in the same transaction, modelling the boundary a real PSP would have per §3.1) and `refund()` (idempotent by predicate, same idiom as `holds.service.js#releaseHold`). Neither throws `DomainError` — nothing calls this module over HTTP; it's a transaction participant for P4-2/P4-8 to call into. `assertTransition()` deliberately NOT touched here (D-40 named `confirmBooking`/`cascadeOffer` as its call sites, not payments). Test lives in `tests/e2e/payments.test.js`, not `tests/unit/` (D-43) — no bookings module exists yet, so the test constructs its own minimal, valid `bookings` row directly via `pool.query` against real FK constraints. **Falsified live**: dropped `markPaymentRefunded`'s `AND status = 'CAPTURED'` guard, the double-refund test correctly failed (a row came back instead of `null`); restored. `npm run test:unit` 31/31, `npm run test:e2e` 10/10 (4 files), `npm run lint` clean. **P4-1 complete, 1/9.** | P4-4 |
| 2026-08-24 | Ordering correction (user directive) + `/next-task` → P4-2 | 1 (see commit hash reported in chat) | User corrected the prior session's plan: skip P4-4/P4-5's full builds for now, inline placeholder reference/QR generation in `confirmBooking()` instead (tracked as Phase 4 debt), go straight to P4-2 then P4-8's cancellation half. Updated the Phase 4 ordering note and per-row annotations in `docs/BUILD_LOG.md` to match before starting. Plan mode used for P4-2 itself per CLAUDE.md's Phase 3/4/5 rule. Built the `bookings` module (`bookings.queries.js`/`bookings.service.js`/`bookings.controller.js`/`bookings.routes.js`) — `confirmBooking()` carries the numbered WALKTHROUGH comment CLAUDE.md mandates for this exact function (one of its four named hard mechanisms), and `confirmHeldSeats()` in the queries file carries its own, covering both a double-submit race and a confirm-vs-explicit-release race, composing with P3-4's already-idempotent `releaseHold()` with zero changes on either side. **D-40 made good on**: `assertTransition(HELD, BOOKED)` wired in immediately before the atomic UPDATE (D-44 records the design nuance — trivially true today, earns its keep once Phase 5's offer-accept reuses the same shape with a different `fromState`). `markSeatHoldReleased()` reused as-is for the `'CONVERTED'` outcome (widened JSDoc union, no new query). §6.6's two booking-blocked scenarios (confirm at `expires_at+1ms`, 20 parallel confirms) built and proven in the new `tests/e2e/bookingFlow.test.js`, previously tracked as blocked in that table. **Falsified live**: dropped `confirmHeldSeats()`'s `AND expires_at > now()`, the TTL test correctly failed (`201` instead of `410`); restored. `npm run test:unit` 31/31, `npm run test:e2e` 13/13 (5 files), `npm run lint` clean. **P4-2 complete, 2/9.** Per the user's explicit instruction, stopped here without starting P4-8. | P4-8 (cancellation half only) |
| 2026-08-24 | `/next-task` → P4-8 (cancellation half) | 1 (see commit hash reported in chat) | User refined the plan mid-turn before approval: freed seats must come back grouped by `categoryId`, and the function's shape must let P5-3 slot in without a restructure. Plan mode used per CLAUDE.md's Phase 3/4/5 rule. Grew the `bookings` module with `cancelBooking()` — same unconditional-calls idempotency style as `holds.service.js#releaseHold()` (P3-4), not an early-return guard: `markBookingCancelled()`/`payments.service.js#refund()`/`releaseBookedSeats()` all run every call, each step's own predicate is what makes a repeat call a no-op. Freed seats grouped into `Map<categoryId, seats[]>` before returning, with a WHY comment at the exact grouping point naming where Phase 5's P5-3 inserts its `OFFER_RESERVED` branch and citing D-7 (seats never re-enter the public pool during an offer window) for why that branch has to exist at all. **A third `assertTransition` call site**: `assertTransition(BOOKED, AVAILABLE)`, extending D-40/D-44's reasoning rather than repeating it verbatim (D-45). New `tests/e2e/bookingCancel.test.js`: happy path (refund, release, grouped response), idempotent double-cancel (composing with P4-1's already-falsified `refund()` guard), non-owner 403. **Falsified live**: dropped `markBookingCancelled()`'s `AND status='CONFIRMED'` guard, the double-cancel test's second-call assertion correctly failed; restored, confirmed via `git diff` (not just `git status`, since this file already existed from P4-2) that only the intended new functions differ from HEAD. `npm run test:unit` 31/31, `npm run test:e2e` 16/16 (6 files), `npm run lint` clean. **P4-8 (cancellation half) complete, 3/9.** Per the Phase 4 ordering note, all of Phase 5 comes next, before any of P4-3/P4-4/P4-5/P4-6/P4-7/P4-9. | P5-1 |
| 2026-08-24 | `/next-task` → P5-1 | 1 (see commit hash reported in chat) | Re-read §7 in full before planning, per instruction — did not work from memory on the `reserved_until` vs `expires_at` distinction (not yet needed by this task, but confirmed before ruling it out). Plan mode used. Built `waitlist.service.js#joinWaitlist()`: gates on zero effective availability via a new `waitlist.queries.js#countEffectiveAvailableSeats()`, which reuses `seatmap.queries.js#EFFECTIVE_STATE_CASE` (exported for the first time, rather than duplicating Layer 1's CASE expression in a second file) instead of raw stored state — so a category whose only occupant is a stale expired `HELD` row correctly reads as available, matching §5.1 even where the read isn't the seat map. `MAX_SEATS_PER_BOOKING` enforced the same way `createHold()` does (D-41's precedent). Unique-violation caught into a new `AlreadyWaitlistedError` (409, `ALREADY_WAITLISTED` — one of §9's named codes, its first thrower). **Scope call flagged and approved in the plan**: pulled `findQueuePosition()` (the literal §7.1 `ROW_NUMBER()` query) forward from P5-2 since §7.1 says the join response itself "includes live position" — P5-2 reuses this same function for `GET /waitlist/me` rather than rebuilding it. **User's review addition incorporated**: a 4th test proves position renumbers over a gap — 2 users join, the first is flipped to `CANCELLED` directly via SQL (no leave endpoint exists yet), a 3rd user joins and lands at position 2 of 2, not 3 of 3, confirming `ROW_NUMBER()` is computed over the live `WAITING` set rather than a stored counter that would need to be kept in sync (the entire reason this design stores no position column). **Falsified live** (same convention as every prior task): temporarily short-circuited the availability-check branch, re-ran `waitlist.test.js` alone — exactly the "available category" test failed, the other 3 passed; restored, confirmed via `grep` no trace remained. **Real bug found and fixed**: `findQueuePosition()`'s `ROW_NUMBER()`/`COUNT(*) OVER()` came back as JS strings — `pg`'s default `bigint` handling — failing both position/total assertions until cast to `::int` inside the subquery (D-46), the same pattern `countShowSeats()`/`countEffectiveAvailableSeats()` already use. `npm run test:unit` 31/31, `npm run test:e2e` 20/20 (7 files), `npm run lint` clean. **P5-1 complete, 1/8.** | P5-2 |
| 2026-08-24 | `/next-task` → P5-2 | 1 (see commit hash reported in chat) | User approved the plan and directed one scope cut: leave `DELETE /shows/:id/waitlist` out entirely — it's in §9's endpoint list but was never given its own BUILD_LOG task, so build only what P5-2's own row names. User also locked in, ahead of time, the transaction boundary P5-3 (next, and the most delicate task left) must respect: booking cancel + seats → `OFFER_RESERVED` + `waitlist_offers` insert + entry `WAITING → OFFERED` all in ONE transaction, since a committed cancel with a rolled-back offer would leave seats reserved for nobody, invisible to the seatmap and unrecoverable by any layer built so far — saved to project memory so it isn't lost by the time P5-3 starts. Built `waitlist.queries.js#findEntryForUser()` (the user's entry in whatever status it's actually in, unlike `findQueuePosition()`'s WAITING-only filter) and `waitlist.service.js#getMyWaitlistStatus()` (`NotFoundError` if no entry; `position`/`total` null unless `WAITING`, since `findQueuePosition()`'s subquery only ranks that set). `GET /me` takes `categoryId` as a query param — the design call flagged in the plan — since the unique constraint is per-category, so a user's waitlist membership for a show is only unambiguous once a category is named. 4 new tests, including the task's own literal exit criterion (position correct after mid-queue removal), proven via 2 successive `GET /me` calls for the SAME user, not a fresh join. **Falsified live**: removed the `status !== 'WAITING'` early return with no compensating null-check, re-ran `waitlist.test.js` alone — exactly the "nulls position/total" test failed, with a `500`/`TypeError`, not a wrong value; restored, confirmed via re-read. `npm run test:unit` 31/31, `npm run test:e2e` 24/24 (7 files), `npm run lint` clean. **P5-2 complete, 2/8.** | P5-3 |
| 2026-08-24 | `/next-task` → P5-3 | 1 (see commit hash reported in chat) | Re-read §7 in full before planning (§5/§6 relied on from earlier tasks this same session, already cited via D-14/D-40/D-45). User locked in, ahead of the plan, the transaction boundary this task lives or dies by (already saved to project memory the prior turn): booking cancel + seat transition + offer insert + entry status flip all commit or roll back as ONE unit. Restructured `cancelBooking()`'s single blanket `releaseBookedSeats()` into a per-category loop: a new `findBookedSeatsByBooking()` read, then per category `waitlist.queries.js#claimNextWaitingEntry()` (the literal §7.4 `FOR UPDATE SKIP LOCKED` query, built now so P5-6's cascade reuses it) decides AVAILABLE (`releaseBookedSeatsForCategory()`, scoped per category) vs `OFFER_RESERVED` (new `offers.service.js#createInitialOffer()` / `offers.queries.js`, D-14's `reserved_until` formula, a fourth `assertTransition` call site — D-48 records the naming nuance against D-40's anticipated `cascadeOffer()`). Flagged and approved scope cuts: a placeholder offer token (D-49, same precedent as P4-2) and deferring `job_queue`/`outbox_events`/`pg_notify` (D-50, mirroring P3-5/P4-6/P4-7/P3-6's own already-deferred status) — both now tracked in the new "Phase 5 debt" section. **User's two mid-plan test additions, both built exactly as specified**: (1) `reserved_until` asserted via the literal D-14 formula against a known small `offerTtlSeconds` (extended `buildBookableShow()` with an optional param for this), strictly greater than `expires_at` — catches a formula that accidentally collapsed the two into the same value, which every other assertion in the file would have missed; (2) a stranger's real `POST /holds` against the fresh `OFFER_RESERVED` seat → `409 SEATS_UNAVAILABLE` — D-7 proven as a request, not a comment. **User's mid-turn atomicity requirement**: `vi.spyOn` on `offers.queries.js#insertWaitlistOffer` (not the pool/client — a different concern from the concurrency suite's "never mock the pool" rule) forces a failure INSIDE the transaction after the booking was already marked CANCELLED and the seat already flipped in-memory; confirmed via direct DB reads that booking/seat/payment/entry all remain exactly as they were before cancellation was attempted, and no `waitlist_offers` row exists. **Falsified live, beyond the mock-based proof itself**: temporarily split `cancelBooking()` into two separate `withTransaction()` calls — the atomicity test's booking-status assertion correctly failed (`'CANCELLED'` instead of `'CONFIRMED'`), proving the test genuinely depends on the single-transaction boundary; restored, confirmed via re-read. Also added a multi-category-booking test (one category resolves to `OFFER_RESERVED`, its sibling to `AVAILABLE`, in the same cancel call) via a new local `buildTwoCategoryShow()` helper, kept file-local rather than added to shared fixtures since nothing else needs two categories yet. `npm run test:unit` 31/31, `npm run test:e2e` 28/28 (7 files), `npm run lint` clean. **P5-3 complete, 3/8.** | P5-4 |
| 2026-08-24 | Phase 5 scope decision (user directive, no code) | 1 (see commit hash reported in chat) | User directed stopping Phase 5 at P5-5: P5-6 (cascade), P5-7 (offer reconciler cron), P5-8 (10-way offer race test) dropped outright, not deferred — no later task in the ledger picks them up, unlike every prior reorder in this project. Recorded in the Phase 5 table (rows marked ❌, exit criteria annotated) and "Phase 5 debt": `claimNextWaitingEntry()` (already built at P5-3) is the one reusable piece of §7.4's design that exists; the `OFFER_EXPIRY` job/handler that would trigger it does not and now will not. Also recorded the user's next 3 tasks in order (P4-7 redefined to send after commit with no outbox dependency, `client/index.html` as a single vanilla file with no build step, then `GET /events/:id/summary`) as forward context, not yet started. **Flagged, not silently applied**: `client/index.html` conflicts with CLAUDE.md's locked client stack (React 18 + Vite + Router + TanStack Query + Zustand + Tailwind) — CLAUDE.md's own text says to stop and ask when chat instructions conflict with the locked stack, so this was surfaced back to the user rather than built on the assumption it overrides Phase 7's plan. | P5-4 |
| 2026-08-24 | `/next-task` → P5-4 | 1 (see commit hash reported in chat) | Continued the P5-4 plan already laid out before the scope-decision message landed (no objection raised to it). Replaced P5-3's placeholder token (`generatePlaceholderTokenHash()`) with `offers.service.js#generateOfferToken()` — the literal §7.3 shape. Two non-obvious calls made and logged: D-51 (the offer's own `id` now generated client-side via `crypto.randomUUID()`, since the raw token embeds it and the id must exist BEFORE the row does — `offers.queries.js#insertWaitlistOffer()` grew an explicit `id` param instead of trusting the column default) and D-52 (a plain SHA-256 digest, not a keyed HMAC, despite §7.3's own section heading calling this "HMAC" — the 32 random bytes are the whole secret, there's no second party's shared key to authenticate against, and §7.3's own code sample already shows a plain hash). New `tests/unit/offerToken.test.js` — pure, no DB, matching the file FILE_MANIFEST had already anticipated by name. **Falsified live, twice**: a constant-hash break failed 3 of 5 tests for the right reason; the tamper-detection test specifically survived that break (and a second, more targeted break hashing only the `offerId` prefix) because a "not equal" assertion between two independently-computed real hashes can't distinguish "correct" from "differently wrong" — documented as an inherent property of that one assertion rather than a gap, since the determinism test already pins down correctness directly. Both breaks restored, confirmed via re-read. `npm run test:unit` 36/36, `npm run test:e2e` 28/28 unaffected (P5-3's existing `waitlist_offers` assertions needed no changes). `npm run lint` clean. Resolves D-49 and its Phase 5 debt bullet. **P5-4 complete, 4/8 (5 dropped/remaining as noted above).** | P5-5 |
| 2026-08-24 | Frontend pivot + Phase 5 stop revised (user directive, no code) | 1 (see commit hash reported in chat) | User confirmed the `client/index.html` conflict flagged at the end of the prior turn was a deliberate pivot, not a slip: React 18 + Vite + Router + TanStack Query + Zustand + Tailwind (the original Phase 7 plan) dropped entirely, replaced with one static `client/index.html` served by Express, no build step, no separate deploy target. Gave the exact reasoning to record verbatim — now Decisions Ledger D-53. Updated `CLAUDE.md`'s Stack section (client bullet, the Vite-dev-server hard-constraint line, the `dev:server`/`dev:client` Commands line) since this is a permanent architectural change to the locked stack itself, not a tracked per-task exception. Updated `docs/BUILD_LOG.md`'s Phase 7 table: the original 10 tasks struck through and marked dropped, P7-1 repurposed for the actual `client/index.html` deliverable rather than left stale alongside a new row. Also revised the Phase 5 scope decision from the prior turn: P5-5 (`GET`/`POST .../accept`) is now ALSO dropped, not just P5-6/7/8 — recorded the practical consequence explicitly (a real, hashed, single-use offer token and a real claim-link email will exist, but nothing can ever resolve the link). No code touched this turn — purely `CLAUDE.md`/`docs/BUILD_LOG.md`/`docs/FILE_MANIFEST.md`. | P4-7 |
| 2026-08-24 | `/next-task` (user-directed) → P4-7 | 1 (see commit hash reported in chat) | User's own detailed spec ("Start at P4-7") taken as approval to proceed directly, given Phase 4's plan-mode rule and the level of detail already given. Installed `nodemailer`/`ejs`/`qrcode`. Built `mail/mailer.js` (lazy-memoised Ethereal transport — `createTestAccount()` called once per process, not once per email; `sendBookingConfirmedEmail()` renders a real QR PNG from `booking.qrToken` via CID; `sendWaitlistOfferEmail()` builds the real §7.3 claim link) and two EJS templates. Wired into `bookings.controller.js` fire-and-forget (unawaited, caught, logged) — a real, named correctness trade (D-54) since CLAUDE.md's own "emails go through outbox_events" invariant doesn't hold here; P4-6 marked ❌ Dropped as the direct consequence, not left dangling as "deferred." `cancelBooking()`'s return grew `offersCreated` so the controller has what it needs (recipient, token, offer) to email each category that got routed to a waiting entry. New `tests/e2e/mail.test.js` calls `mailer.js` directly rather than through HTTP, since the controller's own sends are unawaited by design and there'd be nothing to deterministically wait on through that path — genuinely hits Ethereal (no mock; same "test against reality" convention as the rest of this project, extended to the mail transport). **Verified beyond the dedicated test**: the same run's log shows 5 total Ethereal preview URLs — the 2 the test calls directly, plus 3 fired automatically by the controller during the ordinary `confirm`/`cancel` HTTP calls those same tests also make — confirming the production wiring fires, not just the module in isolation. **Falsified live**: broke `QRCode.toBuffer()`'s input, the booking-confirmation test correctly failed while the waitlist-offer test (no QR involved) stayed green; restored, confirmed via re-read. `npm run test:unit` 36/36, `npm run test:e2e` 30/30 (8 files), `npm run lint` clean. **P4-7 complete.** | client/index.html |
| 2026-08-24 | New session, ~6h deadline — verified P4-7 and the D-53 stack pivot were already committed (`735e6cc`/`3524c63`, working tree clean), then worked the user's 3-item queue | 1 (see commit hash reported in chat) | Session opened by re-checking `git log`/`git status` before doing anything (per standing practice) — both the CLAUDE.md client-stack rewrite and P4-7's email module were already on `phase/4-booking-qr`, matching the prior session's log rows above; no rework needed there. Found and closed a real gap while planning the client: `db_bookings`/`db_events`/`db_shows` etc. in the dev DB had **zero published events** (278 stray users / 93 venues / 93 events from earlier live-proof sessions, all unpublished) — the client would have had nothing to browse. Grew `server/src/db/seed.js` with `seedDemoCatalogue()`: one venue (4×8 seats, 2 categories), 2 published events with 1 published show each, idempotent on the venue's name. Built `client/index.html` (P7-1, see its own row above) plus 3 small backend additions it needed that didn't exist: `GET /events/:eventId/shows` (shows.\*), `GET /bookings/:id/ticket` (PNG QR, bookings.\*), `GET /events/:id/summary` (P8-1, partial — events.\*). Found and fixed a real bug live rather than by inspection: the static-mount path in `app.js` was off by one directory level (`../../../client` resolved above the repo root, 404 on every static request) — caught immediately by curling `/` after wiring it, fixed to `../../client`, re-verified 200. Full manual HTTP flow proven end-to-end against the dev DB: login → browse → seatmap → hold → confirm → QR PNG → cancel → organiser summary (403 for a non-owner customer, correct numbers for the owning organiser) — see P7-1's and P8-1's own rows for the transcripts. `npm run test:unit` 36/36, `npm run test:e2e` 30/30 (8 files, dev server stopped first per `docs/TESTING.md`), `npm run lint` clean — no regressions from the 3 new endpoints or the seed growth. | Commit, push, report the hash |
| 2026-08-24 | Same session, user-directed → P5-5 ("before deploy," 30-minute budget) | 1 (see commit hash reported in chat) | User revived a task the prior session had explicitly marked ❌ Dropped-not-deferred (see the row two above) — noted the reversal in the Phase 5 table rather than silently overwriting the old ❌ row. Built `GET /waitlist/offers/:token` + `POST .../accept`, public and token-authenticated (no `requireAuth` on either route — the token itself is the authorization, matching §7.3), reusing `bookings.service.js#confirmBooking`'s guarded-transaction shape with `fromState` genuinely parameterised for the first time (`assertTransition(OFFER_RESERVED, BOOKED)`, the exact call site Decisions Ledger D-40 anticipated back at P4-2). The real single-use boundary is `offers.queries.js#confirmOfferSeats()` — a `show_seats` UPDATE gated on `state = 'OFFER_RESERVED' AND expires_at > now()` for the offer's exact seat ids, the same atomic-predicate idiom as `confirmHeldSeats()`; `markOfferAccepted()`/`markEntryConverted()` are bookkeeping, not the boundary. Reused `bookings.service.js`'s placeholder reference/QR generators (exported for this) rather than a second copy. **Verified live, not against the test harness** (no automated test written — flagged as a deliberate time-boxing tradeoff, same as P7-1/P8-1): built a real 1-seat venue via the admin/organiser HTTP endpoints, ran the full cancel → offer → email flow, pulled the claim token out of the actual Ethereal preview page (not faked), then exercised `GET`/`POST .../accept` for real — correct seats/price/countdown, the resulting booking's `userId` genuinely the offeree's (not any caller's, since there is no caller identity on this route), DB re-read confirming seat `BOOKED`/offer `ACCEPTED`/entry `CONVERTED`, a sequential second accept → `410 OFFER_INVALID`. **The concurrency proof this project holds atomic guards to**: built a second scenario, then fired two genuinely concurrent `POST .../accept` calls at one fresh token via `Promise.all` — exactly one `200`, one `410`, proving `confirmOfferSeats()`'s predicate (not just the offer's own `status` field, which only gates the sequential case) is what makes this single-use. Did not additionally falsify the predicate live (temporarily strip it, re-run, restore) — skipped under the remaining budget; the concurrent-race result already carries the same evidentiary weight P3-2/P3-3 treat as proof. Also added the claim-link handler to `client/index.html`: `app.js` now serves `index.html` for `/waitlist/claim/:token` too (a client-side path, not a real file — `express.static` alone would 404 it), and the client's boot sequence checks the URL for that shape before deciding whether to call `restoreSession()` (normal login flow) or `openClaim()` (public, no login). `npm run test:unit` 36/36, `npm run test:e2e` 30/30 (8 files) unaffected, `npm run lint` clean. **P5-5 complete.** | Deploy |
| 2026-08-24 | Same session, user-directed — frontend bug fix: `client/index.html`'s cancel flow left a stale QR panel on screen | 1 (see commit hash reported in chat) | User reported cancelling a booking never reset the view. Root cause: `cancelBookingBtn`'s success path only ever wrote a message into `#confError` — it never hid the confirmation section, cleared its fields, or refetched the seat map, so a cancelled booking's QR/reference stayed visibly "confirmed" indefinitely. Audited every other terminal state transition in the file for the same class of bug per the user's explicit request (hold released, hold expired, booking confirmed, logout) and found the SAME gap in two of them: `releaseBtn` and the hold-countdown's expiry branch both called `resetHoldUI()` + `refreshSeatMap()` directly, inconsistently, with no shared name. Added two new functions: `clearConfirmationPanel()` (clears `#confRef`/`#confQr`/`#confSeats`/`#confTotal`/`#confError` and `state.lastBookingId`) and `resetToShowView()` (calls `resetHoldUI()` + `clearConfirmationPanel()`, then — if `state.currentShow` is set — navigates back to `view-seatmap`, refetches the seat map, and resumes polling; falls back to `view-events` if there's no show to return to, e.g. a claim-flow booking, so a cleared panel never sits on screen with nowhere to go). Wired into all three genuinely "return to the show view" actions (cancel, release, hold-expiry) and into `confirmBtn`'s `HOLD_EXPIRED` catch branch. Deliberately NOT wired into "booking confirmed" (leaves view-seatmap for view-confirmation, a different destination — `showConfirmation()` already overwrites every field it owns, so nothing to clear first) or "logout" (same reasoning, plus needs to clear `currentEvent`/`currentShow`/`seats` too, which `resetToShowView()` doesn't own) — both call `resetHoldUI()`/`clearConfirmationPanel()` directly instead, documented inline at each call site rather than silently deviating from "call it from every terminal action." Also fixed the ordering bug this surfaced: `resetHoldUI()` clears `#holdError` as a side effect, so every caller that wants to show a message on the view it lands on has to call `resetToShowView()` FIRST and set the message AFTER, never before (both the cancel and hold-expiry messages were reordered to match). Also fixed a second real bug found while tracing the cancel path: `POST /bookings/:id/cancel` is idempotent and always returns `200`, even `{cancelled: false}` for an already-cancelled booking (`bookings.controller.js`'s own doc comment) — the OLD code treated any 200 as "cancelled and refunded," which was simply false on a no-op. Now branches on `result.cancelled` for the message, and always resets the panel regardless (the booking isn't `CONFIRMED` either way). **Verified**: `node --check` on the extracted inline script (4 times, once per edit pass), `npm run lint` clean. **API contract verified live**, not assumed: booked a real seat, cancelled it twice via curl — first `200 {cancelled:true}`, second `200 {cancelled:false}` — confirming the branch the client now takes matches reality; a non-owner's cancel attempt → `403 FORBIDDEN`, confirming the catch branch (leaves the panel untouched, surfaces the message) fires on a real error, not just in theory. **Could not click through an actual browser session** — no browser-automation tool available in this environment; verification is static (syntax, lint) plus live API-contract tracing, not a real DOM/UI run. Flagged explicitly rather than claimed as fully tested. `npm run test:unit` 36/36 (unaffected — this task touched no server code). `npm run test:e2e`: 28/28 unrelated tests passed; the 2 `tests/e2e/mail.test.js` tests failed on `Invalid login: 535 5.7.8 Authentication failed` against a real Ethereal SMTP account, reproduced 4× across separate runs. Investigated before dismissing: `git status` confirms only `client/index.html` changed (mail.test.js exercises `mail/mailer.js`, never touched); a standalone script creating a fresh Ethereal account and calling `.verify()` succeeded moments later, ruling out a full Ethereal outage. Concluded this is pre-existing external-service flakiness specific to the test harness's transport reuse, unrelated to this change — not investigated further given the fix itself is client-only and this file's own header already frames hitting a real Ethereal account (no mock) as a deliberate realism-over-determinism trade. Logged here rather than silently pushed past. | |

---

## Git checkpoints

Tag every phase completion so a reviewer can inspect clean states.

| Phase | Branch | Merged to `main` | Tag | Notes |
|---|---|---|---|---|
| 0 | `phase/0-foundation` | ✅ | `v0.1.0-phase0` | `/close-phase 0` returned CLEAR on the second pass (2026-08-23); fast-forward merged to `main`, tagged, both pushed |
| 1 | `phase/1-db-auth` | ✅ | `v0.2.0-phase1` | `/close-phase 1` CLEAR on the second pass (2026-08-24, after fixing D-29); fast-forward merged to `main`, tagged, both pushed |
| 2 | `phase/2-venues-shows` | ✅ | `v0.2.5-phase2` | `/close-phase 2` returned CLEAR on the first pass (2026-08-24; one non-blocking nit noted — the generic `CONFLICT` code isn't its own Decisions Ledger entry). Merge itself was missed in that turn and only completed in the next session when `/next-task` for Phase 3 surfaced that `main` was still at the Phase 1 commit; fast-forward merged to `main`, tagged, both pushed then |
| 3 | `phase/3-holds-concurrency` | ✅ | `v0.3.0-phase3` | ⭐ scored. `/close-phase 3` returned CLEAR on the second pass (2026-08-24, after fixing the two coverage gaps and adding D-40/D-41/D-42 — see the audit session log row). Fast-forward merged to `main`, tagged, both pushed. Note: this is a **deliberately partial** phase close — P3-5 through P3-8 (job-queue TTL release, `pg_notify`/`LISTEN` broadcast, cron reconciler, no-distributed-lock writeup) are still ⬜, explicitly deferred to after Phase 5 per the user directive recorded in the Phase 3 table above. Layer 1 (the SQL predicate) is fully proven; Layers 2/3 and the realtime broadcast return once Phase 4/5 also need the job-queue/notify infrastructure they'd otherwise duplicate. |
| 4 | `phase/4-booking-qr` | ⬜ | `v0.4.0-phase4` | |
| 5 | `phase/5-waitlist` | ⬜ | `v0.5.0-phase5` | ⭐ scored |
| 6 | `phase/6-realtime` | ⬜ | `v0.6.0-phase6` | |
| 7 | `phase/7-frontend` | ⬜ | `v0.7.0-phase7` | |
| 8 | `phase/8-reports-checkin` | ⬜ | `v0.8.0-phase8` | |
| 9 | `phase/9-hardening` | ⬜ | `v0.9.0-phase9` | |
| 10 | `phase/10-docs-deploy` | ⬜ | `v1.0.0` | ship |

**Push rules:** push after every completed task, and at the halfway point of any task running past ~40 minutes. Never push a red build under a `feat:` message — use `wip:`. A failed push is reported immediately, never worked around.